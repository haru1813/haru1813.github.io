<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "oracle-plsql-advanced",
  title: "PL/SQL 고급 - 커서, 예외 처리, 프로시저, 함수, 트리거",
  date: "2026-03-03",
  summary: "명시적 커서, 예외 처리, 저장 프로시저·함수, 트리거, 패키지까지 Oracle PL/SQL의 고급 기능을 정리합니다.",
  category: "Coding/Oracle",
  tags: ["oracle","plsql","cursor","exception","procedure","function","trigger","package"],
  content: [
    "## 한 줄 요약",
    "PL/SQL의 **커서·예외 처리**로 안정적인 데이터 처리를 하고, **프로시저·함수·트리거·패키지**로 재사용 가능한 DB 프로그램을 만듭니다.",
    "",
    "---",
    "",
    "## 명시적 커서 (Explicit Cursor)",
    "",
    "커서(Cursor)는 SELECT 결과 집합을 한 행씩 처리하기 위한 포인터입니다.",
    "암시적 커서는 Oracle이 자동으로 관리하지만, **명시적 커서**는 개발자가 직접 선언·열기·읽기·닫기를 제어합니다.",
    "",
    "### 커서 사용 단계",
    "",
    "```sql",
    "-- 1단계: 선언 (DECLARE)",
    "DECLARE",
    "    CURSOR cur_emp IS",
    "        SELECT EMPNO, ENAME, SAL",
    "          FROM EMP",
    "         WHERE DEPTNO = 30;",
    "    v_empno EMP.EMPNO%TYPE;",
    "    v_ename EMP.ENAME%TYPE;",
    "    v_sal   EMP.SAL%TYPE;",
    "BEGIN",
    "    -- 2단계: 열기 (OPEN)",
    "    OPEN cur_emp;",
    "",
    "    -- 3단계: 읽기 (FETCH)",
    "    LOOP",
    "        FETCH cur_emp INTO v_empno, v_ename, v_sal;",
    "        EXIT WHEN cur_emp%NOTFOUND;",
    "        DBMS_OUTPUT.PUT_LINE(v_empno || ' | ' || v_ename || ' | ' || v_sal);",
    "    END LOOP;",
    "",
    "    -- 4단계: 닫기 (CLOSE)",
    "    CLOSE cur_emp;",
    "END;",
    "/",
    "```",
    "",
    "| 단계 | 명령 | 설명 |",
    "| --- | --- | --- |",
    "| 선언 | `CURSOR 커서명 IS SELECT ...` | 사용할 쿼리를 정의 |",
    "| 열기 | `OPEN 커서명` | 쿼리를 실행하고 결과 집합 생성 |",
    "| 읽기 | `FETCH 커서명 INTO 변수` | 한 행을 변수에 저장 |",
    "| 닫기 | `CLOSE 커서명` | 커서가 사용한 메모리 해제 |",
    "",
    "---",
    "",
    "## 커서 속성 (Cursor Attributes)",
    "",
    "| 속성 | 설명 |",
    "| --- | --- |",
    "| `%NOTFOUND` | 가장 최근 FETCH가 행을 가져오지 못하면 TRUE |",
    "| `%FOUND` | 가장 최근 FETCH가 행을 가져오면 TRUE |",
    "| `%ROWCOUNT` | 지금까지 FETCH한 행의 수 |",
    "| `%ISOPEN` | 커서가 열려 있으면 TRUE |",
    "",
    "```sql",
    "DECLARE",
    "    CURSOR cur_emp IS",
    "        SELECT EMPNO, ENAME, SAL FROM EMP;",
    "    v_emp EMP%ROWTYPE;",
    "BEGIN",
    "    OPEN cur_emp;",
    "",
    "    LOOP",
    "        FETCH cur_emp INTO v_emp.EMPNO, v_emp.ENAME, v_emp.SAL;",
    "        EXIT WHEN cur_emp%NOTFOUND;",
    "",
    "        DBMS_OUTPUT.PUT_LINE(",
    "            cur_emp%ROWCOUNT || '번째: ' ||",
    "            v_emp.ENAME || ' - ' || v_emp.SAL",
    "        );",
    "    END LOOP;",
    "",
    "    DBMS_OUTPUT.PUT_LINE('총 ' || cur_emp%ROWCOUNT || '건 조회');",
    "",
    "    CLOSE cur_emp;",
    "",
    "    IF NOT cur_emp%ISOPEN THEN",
    "        DBMS_OUTPUT.PUT_LINE('커서가 닫혔습니다.');",
    "    END IF;",
    "END;",
    "/",
    "```",
    "",
    "---",
    "",
    "## 커서 FOR 루프 (자동 OPEN/FETCH/CLOSE)",
    "",
    "명시적 커서를 가장 간편하게 사용하는 방법입니다.",
    "",
    "```sql",
    "DECLARE",
    "    CURSOR cur_emp IS",
    "        SELECT EMPNO, ENAME, JOB, SAL",
    "          FROM EMP",
    "         WHERE DEPTNO = 20;",
    "BEGIN",
    "    FOR rec IN cur_emp LOOP",
    "        DBMS_OUTPUT.PUT_LINE(",
    "            rec.EMPNO || ' | ' || rec.ENAME || ' | ' ||",
    "            rec.JOB || ' | ' || rec.SAL",
    "        );",
    "    END LOOP;",
    "END;",
    "/",
    "```",
    "",
    "- `FOR rec IN 커서명 LOOP` 형태로 사용합니다.",
    "- OPEN, FETCH, CLOSE를 **자동으로** 처리해 줍니다.",
    "- `rec`는 커서가 반환하는 컬럼들을 가진 레코드입니다.",
    "",
    "### 인라인 서브쿼리 커서 FOR 루프",
    "",
    "```sql",
    "BEGIN",
    "    FOR rec IN (SELECT DEPTNO, COUNT(*) AS CNT, AVG(SAL) AS AVG_SAL",
    "                  FROM EMP",
    "                 GROUP BY DEPTNO",
    "                 ORDER BY DEPTNO) LOOP",
    "        DBMS_OUTPUT.PUT_LINE(",
    "            '부서 ' || rec.DEPTNO ||",
    "            ' | 인원: ' || rec.CNT ||",
    "            ' | 평균급여: ' || ROUND(rec.AVG_SAL, 2)",
    "        );",
    "    END LOOP;",
    "END;",
    "/",
    "```",
    "",
    "- 커서를 미리 선언하지 않고 SELECT문을 직접 괄호 안에 작성할 수 있습니다.",
    "",
    "---",
    "",
    "## 매개변수가 있는 커서 (Parameterized Cursor)",
    "",
    "커서에 매개변수를 전달하여 동적으로 조건을 변경할 수 있습니다.",
    "",
    "```sql",
    "DECLARE",
    "    CURSOR cur_emp(p_deptno NUMBER) IS",
    "        SELECT EMPNO, ENAME, SAL",
    "          FROM EMP",
    "         WHERE DEPTNO = p_deptno;",
    "BEGIN",
    "    DBMS_OUTPUT.PUT_LINE('=== 10번 부서 ===');",
    "    FOR rec IN cur_emp(10) LOOP",
    "        DBMS_OUTPUT.PUT_LINE(rec.EMPNO || ' | ' || rec.ENAME || ' | ' || rec.SAL);",
    "    END LOOP;",
    "",
    "    DBMS_OUTPUT.PUT_LINE('=== 20번 부서 ===');",
    "    FOR rec IN cur_emp(20) LOOP",
    "        DBMS_OUTPUT.PUT_LINE(rec.EMPNO || ' | ' || rec.ENAME || ' | ' || rec.SAL);",
    "    END LOOP;",
    "END;",
    "/",
    "```",
    "",
    "- `CURSOR 커서명(매개변수 타입) IS SELECT ...` 형태로 선언합니다.",
    "- 같은 커서를 다른 인수로 여러 번 재사용할 수 있어 편리합니다.",
    "",
    "### 여러 매개변수 사용",
    "",
    "```sql",
    "DECLARE",
    "    CURSOR cur_emp(p_deptno NUMBER, p_min_sal NUMBER) IS",
    "        SELECT EMPNO, ENAME, SAL",
    "          FROM EMP",
    "         WHERE DEPTNO = p_deptno",
    "           AND SAL >= p_min_sal;",
    "BEGIN",
    "    FOR rec IN cur_emp(30, 1500) LOOP",
    "        DBMS_OUTPUT.PUT_LINE(rec.ENAME || ': ' || rec.SAL);",
    "    END LOOP;",
    "END;",
    "/",
    "```",
    "",
    "---",
    "",
    "## SYS_REFCURSOR",
    "",
    "SYS_REFCURSOR는 커서를 변수로 다루는 **커서 참조(REF CURSOR)** 타입입니다.",
    "프로시저에서 결과 집합을 반환할 때 자주 사용합니다.",
    "",
    "```sql",
    "DECLARE",
    "    v_cur SYS_REFCURSOR;",
    "    v_empno EMP.EMPNO%TYPE;",
    "    v_ename EMP.ENAME%TYPE;",
    "    v_sal   EMP.SAL%TYPE;",
    "BEGIN",
    "    OPEN v_cur FOR",
    "        SELECT EMPNO, ENAME, SAL FROM EMP WHERE DEPTNO = 10;",
    "",
    "    LOOP",
    "        FETCH v_cur INTO v_empno, v_ename, v_sal;",
    "        EXIT WHEN v_cur%NOTFOUND;",
    "        DBMS_OUTPUT.PUT_LINE(v_empno || ' | ' || v_ename || ' | ' || v_sal);",
    "    END LOOP;",
    "",
    "    CLOSE v_cur;",
    "END;",
    "/",
    "```",
    "",
    "- `OPEN 커서변수 FOR SELECT문`으로 실행할 쿼리를 동적으로 지정합니다.",
    "- 프로시저의 OUT 매개변수로 SYS_REFCURSOR를 넘기면 호출 측에서 결과를 처리할 수 있습니다.",
    "",
    "---",
    "",
    "## 예외 처리 (Exception Handling)",
    "",
    "PL/SQL에서는 런타임 에러가 발생해도 `EXCEPTION` 블록에서 처리할 수 있습니다.",
    "",
    "### 기본 구조",
    "",
    "```sql",
    "DECLARE",
    "    v_ename EMP.ENAME%TYPE;",
    "BEGIN",
    "    SELECT ENAME INTO v_ename",
    "      FROM EMP",
    "     WHERE EMPNO = 9999;",
    "",
    "    DBMS_OUTPUT.PUT_LINE('이름: ' || v_ename);",
    "EXCEPTION",
    "    WHEN NO_DATA_FOUND THEN",
    "        DBMS_OUTPUT.PUT_LINE('해당 사번의 사원이 존재하지 않습니다.');",
    "END;",
    "/",
    "```",
    "",
    "### 주요 사전 정의 예외",
    "",
    "| 예외 이름 | 설명 |",
    "| --- | --- |",
    "| `NO_DATA_FOUND` | SELECT INTO 결과가 0행 |",
    "| `TOO_MANY_ROWS` | SELECT INTO 결과가 2행 이상 |",
    "| `ZERO_DIVIDE` | 0으로 나누기 |",
    "| `VALUE_ERROR` | 산술, 변환, 절삭 오류 |",
    "| `INVALID_CURSOR` | 잘못된 커서 연산 |",
    "| `CURSOR_ALREADY_OPEN` | 이미 열린 커서를 다시 OPEN |",
    "| `DUP_VAL_ON_INDEX` | UNIQUE 제약 위반 |",
    "| `LOGIN_DENIED` | 잘못된 로그인 정보 |",
    "| `OTHERS` | 위에서 처리되지 않은 모든 예외 |",
    "",
    "### 여러 예외 처리",
    "",
    "```sql",
    "DECLARE",
    "    v_ename EMP.ENAME%TYPE;",
    "    v_result NUMBER;",
    "BEGIN",
    "    SELECT ENAME INTO v_ename",
    "      FROM EMP",
    "     WHERE DEPTNO = 10;",
    "",
    "    v_result := 100 / 0;",
    "EXCEPTION",
    "    WHEN NO_DATA_FOUND THEN",
    "        DBMS_OUTPUT.PUT_LINE('데이터가 없습니다.');",
    "    WHEN TOO_MANY_ROWS THEN",
    "        DBMS_OUTPUT.PUT_LINE('너무 많은 행이 반환되었습니다.');",
    "    WHEN ZERO_DIVIDE THEN",
    "        DBMS_OUTPUT.PUT_LINE('0으로 나눌 수 없습니다.');",
    "    WHEN OTHERS THEN",
    "        DBMS_OUTPUT.PUT_LINE('기타 에러: ' || SQLERRM);",
    "END;",
    "/",
    "```",
    "",
    "---",
    "",
    "## SQLCODE와 SQLERRM",
    "",
    "| 함수 | 설명 |",
    "| --- | --- |",
    "| `SQLCODE` | 에러 코드 번호 반환 (정상 실행: 0) |",
    "| `SQLERRM` | 에러 메시지 문자열 반환 |",
    "",
    "```sql",
    "DECLARE",
    "    v_ename EMP.ENAME%TYPE;",
    "BEGIN",
    "    SELECT ENAME INTO v_ename FROM EMP;",
    "EXCEPTION",
    "    WHEN OTHERS THEN",
    "        DBMS_OUTPUT.PUT_LINE('에러 코드: ' || SQLCODE);",
    "        DBMS_OUTPUT.PUT_LINE('에러 메시지: ' || SQLERRM);",
    "END;",
    "/",
    "```",
    "",
    "- `WHEN OTHERS`에서 `SQLCODE`와 `SQLERRM`을 활용하면 예상하지 못한 에러도 로깅할 수 있습니다.",
    "",
    "---",
    "",
    "## 사용자 정의 예외 (User-Defined Exception)",
    "",
    "비즈니스 로직에 맞는 예외를 직접 정의하고 발생시킬 수 있습니다.",
    "",
    "```sql",
    "DECLARE",
    "    e_sal_too_low EXCEPTION;",
    "    v_sal EMP.SAL%TYPE;",
    "BEGIN",
    "    SELECT SAL INTO v_sal",
    "      FROM EMP",
    "     WHERE EMPNO = 7369;",
    "",
    "    IF v_sal < 1000 THEN",
    "        RAISE e_sal_too_low;",
    "    END IF;",
    "",
    "    DBMS_OUTPUT.PUT_LINE('급여: ' || v_sal);",
    "EXCEPTION",
    "    WHEN e_sal_too_low THEN",
    "        DBMS_OUTPUT.PUT_LINE('급여가 1000 미만입니다. 급여 인상이 필요합니다.');",
    "END;",
    "/",
    "```",
    "",
    "- `예외변수 EXCEPTION`으로 선언하고, `RAISE 예외변수`로 발생시킵니다.",
    "",
    "### RAISE_APPLICATION_ERROR",
    "",
    "Oracle에서 제공하는 에러 발생 프로시저로, 에러 코드와 메시지를 직접 지정합니다.",
    "",
    "```sql",
    "DECLARE",
    "    v_sal EMP.SAL%TYPE;",
    "BEGIN",
    "    SELECT SAL INTO v_sal FROM EMP WHERE EMPNO = 7369;",
    "",
    "    IF v_sal < 1000 THEN",
    "        RAISE_APPLICATION_ERROR(-20001, '급여가 기준 미달입니다: ' || v_sal);",
    "    END IF;",
    "EXCEPTION",
    "    WHEN OTHERS THEN",
    "        DBMS_OUTPUT.PUT_LINE(SQLCODE || ': ' || SQLERRM);",
    "END;",
    "/",
    "```",
    "",
    "- 에러 코드 범위: **-20000 ~ -20999** (사용자 정의 영역)",
    "- 호출 측에서 해당 에러 코드를 구분하여 처리할 수 있습니다.",
    "",
    "---",
    "",
    "## 저장 프로시저 (Stored Procedure)",
    "",
    "자주 사용하는 PL/SQL 블록을 데이터베이스에 저장하여 재사용할 수 있습니다.",
    "",
    "### 기본 프로시저",
    "",
    "```sql",
    "CREATE OR REPLACE PROCEDURE proc_hello",
    "IS",
    "BEGIN",
    "    DBMS_OUTPUT.PUT_LINE('Hello Stored Procedure!');",
    "END;",
    "/",
    "```",
    "",
    "실행 방법:",
    "",
    "```sql",
    "EXECUTE proc_hello;",
    "-- 또는",
    "BEGIN",
    "    proc_hello;",
    "END;",
    "/",
    "```",
    "",
    "### IN 매개변수 (입력)",
    "",
    "```sql",
    "CREATE OR REPLACE PROCEDURE proc_emp_info(",
    "    p_empno IN EMP.EMPNO%TYPE",
    ")",
    "IS",
    "    v_ename EMP.ENAME%TYPE;",
    "    v_job   EMP.JOB%TYPE;",
    "    v_sal   EMP.SAL%TYPE;",
    "BEGIN",
    "    SELECT ENAME, JOB, SAL",
    "      INTO v_ename, v_job, v_sal",
    "      FROM EMP",
    "     WHERE EMPNO = p_empno;",
    "",
    "    DBMS_OUTPUT.PUT_LINE('사번: ' || p_empno);",
    "    DBMS_OUTPUT.PUT_LINE('이름: ' || v_ename);",
    "    DBMS_OUTPUT.PUT_LINE('직책: ' || v_job);",
    "    DBMS_OUTPUT.PUT_LINE('급여: ' || v_sal);",
    "EXCEPTION",
    "    WHEN NO_DATA_FOUND THEN",
    "        DBMS_OUTPUT.PUT_LINE('해당 사번(' || p_empno || ')의 사원이 없습니다.');",
    "END;",
    "/",
    "```",
    "",
    "```sql",
    "EXECUTE proc_emp_info(7788);",
    "```",
    "",
    "- `IN`은 기본값이므로 생략 가능합니다 (`p_empno EMP.EMPNO%TYPE`과 동일).",
    "",
    "### OUT 매개변수 (출력)",
    "",
    "```sql",
    "CREATE OR REPLACE PROCEDURE proc_get_sal(",
    "    p_empno IN  EMP.EMPNO%TYPE,",
    "    p_ename OUT EMP.ENAME%TYPE,",
    "    p_sal   OUT EMP.SAL%TYPE",
    ")",
    "IS",
    "BEGIN",
    "    SELECT ENAME, SAL",
    "      INTO p_ename, p_sal",
    "      FROM EMP",
    "     WHERE EMPNO = p_empno;",
    "END;",
    "/",
    "```",
    "",
    "호출 예:",
    "",
    "```sql",
    "DECLARE",
    "    v_ename EMP.ENAME%TYPE;",
    "    v_sal   EMP.SAL%TYPE;",
    "BEGIN",
    "    proc_get_sal(7788, v_ename, v_sal);",
    "    DBMS_OUTPUT.PUT_LINE(v_ename || ': ' || v_sal);",
    "END;",
    "/",
    "```",
    "",
    "- `OUT` 매개변수는 프로시저 내부에서 값을 대입하면 호출 측 변수에 값이 전달됩니다.",
    "",
    "### IN OUT 매개변수 (입출력)",
    "",
    "```sql",
    "CREATE OR REPLACE PROCEDURE proc_double(",
    "    p_num IN OUT NUMBER",
    ")",
    "IS",
    "BEGIN",
    "    p_num := p_num * 2;",
    "END;",
    "/",
    "```",
    "",
    "```sql",
    "DECLARE",
    "    v_num NUMBER := 5;",
    "BEGIN",
    "    DBMS_OUTPUT.PUT_LINE('변경 전: ' || v_num);",
    "    proc_double(v_num);",
    "    DBMS_OUTPUT.PUT_LINE('변경 후: ' || v_num);",
    "END;",
    "/",
    "```",
    "",
    "- `IN OUT`은 값을 전달받고, 프로시저 내에서 변경된 값을 돌려줍니다.",
    "",
    "### 프로시저 삭제",
    "",
    "```sql",
    "DROP PROCEDURE proc_hello;",
    "```",
    "",
    "---",
    "",
    "## 저장 함수 (Stored Function)",
    "",
    "프로시저와 유사하지만, **RETURN 값**이 있어 SQL문 안에서 직접 호출할 수 있습니다.",
    "",
    "### 기본 함수",
    "",
    "```sql",
    "CREATE OR REPLACE FUNCTION func_tax(",
    "    p_sal IN NUMBER",
    ") RETURN NUMBER",
    "IS",
    "    v_tax NUMBER;",
    "BEGIN",
    "    v_tax := p_sal * 0.1;",
    "    RETURN v_tax;",
    "END;",
    "/",
    "```",
    "",
    "호출:",
    "",
    "```sql",
    "-- PL/SQL에서 호출",
    "DECLARE",
    "    v_result NUMBER;",
    "BEGIN",
    "    v_result := func_tax(3000);",
    "    DBMS_OUTPUT.PUT_LINE('세금: ' || v_result);",
    "END;",
    "/",
    "",
    "-- SQL에서 직접 호출",
    "SELECT EMPNO, ENAME, SAL, func_tax(SAL) AS TAX",
    "  FROM EMP;",
    "```",
    "",
    "### 여러 계산을 하는 함수",
    "",
    "```sql",
    "CREATE OR REPLACE FUNCTION func_annual_sal(",
    "    p_empno IN EMP.EMPNO%TYPE",
    ") RETURN NUMBER",
    "IS",
    "    v_sal  EMP.SAL%TYPE;",
    "    v_comm EMP.COMM%TYPE;",
    "BEGIN",
    "    SELECT SAL, NVL(COMM, 0)",
    "      INTO v_sal, v_comm",
    "      FROM EMP",
    "     WHERE EMPNO = p_empno;",
    "",
    "    RETURN (v_sal + v_comm) * 12;",
    "EXCEPTION",
    "    WHEN NO_DATA_FOUND THEN",
    "        RETURN -1;",
    "END;",
    "/",
    "```",
    "",
    "```sql",
    "SELECT EMPNO, ENAME, func_annual_sal(EMPNO) AS ANNUAL_SAL",
    "  FROM EMP;",
    "```",
    "",
    "### 프로시저 vs 함수",
    "",
    "| 구분 | 프로시저 (Procedure) | 함수 (Function) |",
    "| --- | --- | --- |",
    "| RETURN 값 | 없음 (OUT 매개변수로 대체) | **반드시 있음** |",
    "| SQL 내 사용 | 불가 | **가능** |",
    "| 호출 방식 | `EXECUTE` 또는 PL/SQL 블록 | PL/SQL 블록 또는 SQL문 |",
    "| 주 용도 | DML 처리, 비즈니스 로직 | 계산, 값 변환 |",
    "",
    "### 함수 삭제",
    "",
    "```sql",
    "DROP FUNCTION func_tax;",
    "```",
    "",
    "---",
    "",
    "## 트리거 (Trigger)",
    "",
    "트리거는 특정 이벤트(INSERT, UPDATE, DELETE)가 발생할 때 **자동으로 실행**되는 PL/SQL 블록입니다.",
    "",
    "### 기본 트리거 (문장 레벨)",
    "",
    "```sql",
    "CREATE OR REPLACE TRIGGER trg_emp_log",
    "AFTER INSERT ON EMP",
    "BEGIN",
    "    DBMS_OUTPUT.PUT_LINE('EMP 테이블에 새 사원이 추가되었습니다. [' ||",
    "        TO_CHAR(SYSDATE, 'YYYY-MM-DD HH24:MI:SS') || ']');",
    "END;",
    "/",
    "```",
    "",
    "- `AFTER INSERT ON EMP`: EMP 테이블에 INSERT가 실행된 **후** 동작합니다.",
    "- 문장 레벨 트리거: INSERT 문 하나당 트리거가 **한 번** 실행됩니다.",
    "",
    "### 행 레벨 트리거 (FOR EACH ROW)",
    "",
    "```sql",
    "CREATE OR REPLACE TRIGGER trg_emp_sal_check",
    "BEFORE INSERT OR UPDATE ON EMP",
    "FOR EACH ROW",
    "BEGIN",
    "    IF :NEW.SAL < 0 THEN",
    "        RAISE_APPLICATION_ERROR(-20002, '급여는 0 이상이어야 합니다.');",
    "    END IF;",
    "END;",
    "/",
    "```",
    "",
    "- `FOR EACH ROW`: 각 행마다 트리거가 실행됩니다.",
    "- `:NEW`: INSERT/UPDATE 시 **새로 들어오는 값**",
    "- `:OLD`: UPDATE/DELETE 시 **기존 값**",
    "",
    "### :NEW와 :OLD 참조",
    "",
    "| 연산 | :OLD | :NEW |",
    "| --- | --- | --- |",
    "| INSERT | 사용 불가 (NULL) | 삽입될 값 |",
    "| UPDATE | 변경 전 값 | 변경 후 값 |",
    "| DELETE | 삭제될 값 | 사용 불가 (NULL) |",
    "",
    "### 로그 테이블에 변경 이력 기록",
    "",
    "```sql",
    "CREATE TABLE EMP_LOG (",
    "    log_id    NUMBER GENERATED ALWAYS AS IDENTITY,",
    "    log_date  DATE DEFAULT SYSDATE,",
    "    log_type  VARCHAR2(10),",
    "    empno     NUMBER,",
    "    old_sal   NUMBER,",
    "    new_sal   NUMBER",
    ");",
    "",
    "CREATE OR REPLACE TRIGGER trg_emp_sal_log",
    "AFTER INSERT OR UPDATE OR DELETE ON EMP",
    "FOR EACH ROW",
    "BEGIN",
    "    IF INSERTING THEN",
    "        INSERT INTO EMP_LOG(log_type, empno, new_sal)",
    "        VALUES('INSERT', :NEW.EMPNO, :NEW.SAL);",
    "    ELSIF UPDATING THEN",
    "        INSERT INTO EMP_LOG(log_type, empno, old_sal, new_sal)",
    "        VALUES('UPDATE', :OLD.EMPNO, :OLD.SAL, :NEW.SAL);",
    "    ELSIF DELETING THEN",
    "        INSERT INTO EMP_LOG(log_type, empno, old_sal)",
    "        VALUES('DELETE', :OLD.EMPNO, :OLD.SAL);",
    "    END IF;",
    "END;",
    "/",
    "```",
    "",
    "- `INSERTING`, `UPDATING`, `DELETING`: 어떤 DML 작업인지 구분하는 조건 함수",
    "- 하나의 트리거에서 INSERT/UPDATE/DELETE를 모두 처리할 수 있습니다.",
    "",
    "### BEFORE vs AFTER",
    "",
    "| 구분 | BEFORE | AFTER |",
    "| --- | --- | --- |",
    "| 실행 시점 | DML 실행 **전** | DML 실행 **후** |",
    "| 값 변경 | `:NEW` 값을 변경 가능 | `:NEW` 값 변경 불가 |",
    "| 주 용도 | 데이터 검증, 기본값 설정 | 로그 기록, 후처리 |",
    "",
    "### BEFORE 트리거로 값 자동 설정",
    "",
    "```sql",
    "CREATE OR REPLACE TRIGGER trg_emp_default",
    "BEFORE INSERT ON EMP",
    "FOR EACH ROW",
    "BEGIN",
    "    IF :NEW.HIREDATE IS NULL THEN",
    "        :NEW.HIREDATE := SYSDATE;",
    "    END IF;",
    "    IF :NEW.COMM IS NULL THEN",
    "        :NEW.COMM := 0;",
    "    END IF;",
    "END;",
    "/",
    "```",
    "",
    "### 트리거 관리",
    "",
    "```sql",
    "-- 트리거 비활성화",
    "ALTER TRIGGER trg_emp_sal_check DISABLE;",
    "",
    "-- 트리거 활성화",
    "ALTER TRIGGER trg_emp_sal_check ENABLE;",
    "",
    "-- 테이블의 모든 트리거 비활성화",
    "ALTER TABLE EMP DISABLE ALL TRIGGERS;",
    "",
    "-- 트리거 삭제",
    "DROP TRIGGER trg_emp_sal_check;",
    "```",
    "",
    "---",
    "",
    "## 패키지 (Package)",
    "",
    "패키지는 관련 있는 프로시저, 함수, 변수, 커서 등을 하나로 묶는 단위입니다.",
    "**명세(Specification)** 와 **본문(Body)** 으로 구성됩니다.",
    "",
    "### 패키지 명세 (Specification / Header)",
    "",
    "```sql",
    "CREATE OR REPLACE PACKAGE pkg_emp",
    "IS",
    "    -- 공개 상수",
    "    c_tax_rate CONSTANT NUMBER := 0.1;",
    "",
    "    -- 공개 프로시저",
    "    PROCEDURE print_emp_info(p_empno IN EMP.EMPNO%TYPE);",
    "",
    "    -- 공개 함수",
    "    FUNCTION get_annual_sal(p_empno IN EMP.EMPNO%TYPE) RETURN NUMBER;",
    "",
    "    -- 오버로딩: 같은 이름, 다른 매개변수",
    "    FUNCTION get_annual_sal(p_sal IN NUMBER, p_comm IN NUMBER) RETURN NUMBER;",
    "END pkg_emp;",
    "/",
    "```",
    "",
    "- 명세에는 **선언부만** 작성합니다 (구현 없음).",
    "- 외부에서 접근 가능한 공개(public) 요소를 정의합니다.",
    "",
    "### 패키지 본문 (Body)",
    "",
    "```sql",
    "CREATE OR REPLACE PACKAGE BODY pkg_emp",
    "IS",
    "    -- 프로시저 구현",
    "    PROCEDURE print_emp_info(p_empno IN EMP.EMPNO%TYPE)",
    "    IS",
    "        v_ename EMP.ENAME%TYPE;",
    "        v_job   EMP.JOB%TYPE;",
    "        v_sal   EMP.SAL%TYPE;",
    "    BEGIN",
    "        SELECT ENAME, JOB, SAL",
    "          INTO v_ename, v_job, v_sal",
    "          FROM EMP",
    "         WHERE EMPNO = p_empno;",
    "",
    "        DBMS_OUTPUT.PUT_LINE('이름: ' || v_ename);",
    "        DBMS_OUTPUT.PUT_LINE('직책: ' || v_job);",
    "        DBMS_OUTPUT.PUT_LINE('급여: ' || v_sal);",
    "        DBMS_OUTPUT.PUT_LINE('세금: ' || (v_sal * c_tax_rate));",
    "    EXCEPTION",
    "        WHEN NO_DATA_FOUND THEN",
    "            DBMS_OUTPUT.PUT_LINE('사원을 찾을 수 없습니다.');",
    "    END print_emp_info;",
    "",
    "    -- 함수 구현 1: 사번으로 연봉 계산",
    "    FUNCTION get_annual_sal(p_empno IN EMP.EMPNO%TYPE) RETURN NUMBER",
    "    IS",
    "        v_sal  EMP.SAL%TYPE;",
    "        v_comm EMP.COMM%TYPE;",
    "    BEGIN",
    "        SELECT SAL, NVL(COMM, 0)",
    "          INTO v_sal, v_comm",
    "          FROM EMP",
    "         WHERE EMPNO = p_empno;",
    "",
    "        RETURN (v_sal + v_comm) * 12;",
    "    END get_annual_sal;",
    "",
    "    -- 함수 구현 2 (오버로딩): 급여와 커미션으로 연봉 계산",
    "    FUNCTION get_annual_sal(p_sal IN NUMBER, p_comm IN NUMBER) RETURN NUMBER",
    "    IS",
    "    BEGIN",
    "        RETURN (p_sal + NVL(p_comm, 0)) * 12;",
    "    END get_annual_sal;",
    "END pkg_emp;",
    "/",
    "```",
    "",
    "- 본문에는 명세에서 선언한 프로시저/함수의 **구현**을 작성합니다.",
    "",
    "### 패키지 호출",
    "",
    "```sql",
    "-- 프로시저 호출",
    "BEGIN",
    "    pkg_emp.print_emp_info(7788);",
    "END;",
    "/",
    "",
    "-- 함수 호출 (오버로딩)",
    "DECLARE",
    "    v_annual1 NUMBER;",
    "    v_annual2 NUMBER;",
    "BEGIN",
    "    v_annual1 := pkg_emp.get_annual_sal(7788);",
    "    v_annual2 := pkg_emp.get_annual_sal(3000, 500);",
    "",
    "    DBMS_OUTPUT.PUT_LINE('사번 7788 연봉: ' || v_annual1);",
    "    DBMS_OUTPUT.PUT_LINE('직접 계산 연봉: ' || v_annual2);",
    "END;",
    "/",
    "",
    "-- 상수 참조",
    "BEGIN",
    "    DBMS_OUTPUT.PUT_LINE('세율: ' || pkg_emp.c_tax_rate);",
    "END;",
    "/",
    "```",
    "",
    "- `패키지명.요소명`으로 호출합니다.",
    "- **오버로딩**: 같은 이름의 함수/프로시저를 매개변수 타입이나 개수를 달리하여 여러 개 정의할 수 있습니다.",
    "",
    "### 패키지 삭제",
    "",
    "```sql",
    "-- 본문만 삭제",
    "DROP PACKAGE BODY pkg_emp;",
    "",
    "-- 명세 + 본문 모두 삭제",
    "DROP PACKAGE pkg_emp;",
    "```",
    "",
    "### 패키지의 장점",
    "",
    "- **모듈화**: 관련 기능을 하나로 그룹핑",
    "- **정보 은닉**: 명세에 선언되지 않은 요소는 외부에서 접근 불가 (private)",
    "- **성능 향상**: 패키지 최초 호출 시 전체가 메모리에 로드되어 이후 호출이 빠름",
    "- **오버로딩**: 같은 이름으로 다양한 매개변수 조합 지원",
    "",
    "---",
    "",
    "## 종합 예제: 사원 관리 프로시저",
    "",
    "```sql",
    "CREATE OR REPLACE PROCEDURE proc_manage_emp(",
    "    p_action IN VARCHAR2,",
    "    p_empno  IN EMP.EMPNO%TYPE,",
    "    p_ename  IN EMP.ENAME%TYPE DEFAULT NULL,",
    "    p_job    IN EMP.JOB%TYPE DEFAULT NULL,",
    "    p_sal    IN EMP.SAL%TYPE DEFAULT NULL,",
    "    p_deptno IN EMP.DEPTNO%TYPE DEFAULT NULL",
    ")",
    "IS",
    "BEGIN",
    "    IF UPPER(p_action) = 'INSERT' THEN",
    "        INSERT INTO EMP(EMPNO, ENAME, JOB, SAL, DEPTNO, HIREDATE)",
    "        VALUES(p_empno, p_ename, p_job, p_sal, p_deptno, SYSDATE);",
    "        DBMS_OUTPUT.PUT_LINE('사원 ' || p_ename || ' 등록 완료');",
    "",
    "    ELSIF UPPER(p_action) = 'UPDATE' THEN",
    "        UPDATE EMP",
    "           SET ENAME  = NVL(p_ename, ENAME),",
    "               JOB    = NVL(p_job, JOB),",
    "               SAL    = NVL(p_sal, SAL),",
    "               DEPTNO = NVL(p_deptno, DEPTNO)",
    "         WHERE EMPNO = p_empno;",
    "        DBMS_OUTPUT.PUT_LINE('사번 ' || p_empno || ' 정보 수정 완료');",
    "",
    "    ELSIF UPPER(p_action) = 'DELETE' THEN",
    "        DELETE FROM EMP WHERE EMPNO = p_empno;",
    "        DBMS_OUTPUT.PUT_LINE('사번 ' || p_empno || ' 삭제 완료');",
    "",
    "    ELSE",
    "        DBMS_OUTPUT.PUT_LINE('잘못된 액션: ' || p_action);",
    "    END IF;",
    "",
    "    COMMIT;",
    "EXCEPTION",
    "    WHEN OTHERS THEN",
    "        ROLLBACK;",
    "        DBMS_OUTPUT.PUT_LINE('에러 발생: ' || SQLERRM);",
    "END;",
    "/",
    "```",
    "",
    "```sql",
    "-- 사원 등록",
    "EXECUTE proc_manage_emp('INSERT', 9999, '테스트', 'CLERK', 2000, 10);",
    "",
    "-- 급여 수정",
    "EXECUTE proc_manage_emp('UPDATE', 9999, p_sal => 2500);",
    "",
    "-- 사원 삭제",
    "EXECUTE proc_manage_emp('DELETE', 9999);",
    "```",
    "",
    "---",
    "",
    "### 요약",
    "",
    "| 항목 | 설명 |",
    "| --- | --- |",
    "| 명시적 커서 | `DECLARE CURSOR` → `OPEN` → `FETCH` → `CLOSE` |",
    "| 커서 속성 | `%NOTFOUND`, `%FOUND`, `%ROWCOUNT`, `%ISOPEN` |",
    "| 커서 FOR 루프 | `FOR rec IN 커서 LOOP` - 자동 처리 |",
    "| 매개변수 커서 | `CURSOR c(p NUMBER) IS SELECT ... WHERE col = p` |",
    "| SYS_REFCURSOR | 동적 커서 참조 타입 |",
    "| 예외 처리 | `EXCEPTION WHEN 예외명 THEN` |",
    "| SQLCODE/SQLERRM | 에러 코드와 메시지 확인 |",
    "| 사용자 정의 예외 | `RAISE`, `RAISE_APPLICATION_ERROR` |",
    "| 프로시저 | `CREATE OR REPLACE PROCEDURE` (IN/OUT/IN OUT) |",
    "| 함수 | `CREATE OR REPLACE FUNCTION` + `RETURN` |",
    "| 트리거 | `CREATE OR REPLACE TRIGGER` (BEFORE/AFTER, :NEW/:OLD) |",
    "| 패키지 | 명세(Specification) + 본문(Body), 오버로딩 지원 |"
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
