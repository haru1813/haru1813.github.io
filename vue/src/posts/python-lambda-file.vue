<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "python-lambda-file",
  title: "Python 람다와 파일 입출력",
  date: "2026-02-17",
  summary: "lambda, map, filter, 튜플, 파일 열기/읽기/쓰기, with 문을 정리합니다.",
  category: "Coding/Python",
  tags: ["python", "lambda", "map", "filter", "파일", "basics"],
  content: [
    "## 람다 (lambda)",
    "한 줄로 쓰는 익명 함수입니다. `lambda 매개변수: 반환값` 형태입니다.",
    "",
    "```python",
    "power = lambda x: x * x",
    "under_3 = lambda x: x < 3",
    "print(power(5))    # 25",
    "print(under_3(2))  # True",
    "```",
    "",
    "## map()",
    "시퀀스의 각 요소에 함수를 적용한 결과를 반환합니다.",
    "",
    "```python",
    "list_a = [1, 2, 3, 4, 5]",
    "output = list(map(lambda x: x * x, list_a))",
    "print(output)  # [1, 4, 9, 16, 25]",
    "```",
    "",
    "## filter()",
    "조건을 만족하는 요소만 걸러냅니다.",
    "",
    "```python",
    "output = list(filter(lambda x: x < 3, list_a))",
    "print(output)  # [1, 2]",
    "```",
    "",
    "## 튜플",
    "리스트와 비슷하지만 `()`로 생성하고 **수정 불가**입니다. 값 교환에 유용합니다.",
    "",
    "```python",
    "a, b = 10, 20",
    "a, b = b, a  # 값 교환",
    "print(a, b)  # 20 10",
    "",
    "def test():",
    "    return (10, 20)",
    "x, y = test()",
    "```",
    "",
    "## 파일 쓰기",
    "```python",
    "file = open(\"basic.txt\", \"w\")",
    "file.write(\"Hello Python Programming...!\")",
    "file.close()",
    "```",
    "",
    "## 파일 읽기",
    "```python",
    "with open(\"basic.txt\", \"r\") as file:",
    "    contents = file.read()",
    "print(contents)",
    "```",
    "",
    "## with 문",
    "`with`를 쓰면 블록을 벗어날 때 파일이 자동으로 닫힙니다. 예외가 나도 안전합니다.",
    "",
    "```python",
    "with open(\"info.txt\", \"w\") as file:",
    "    for i in range(3):",
    "        file.write(f\"{i}번째 줄\\n\")",
    "# 블록 끝에서 file.close() 자동 호출",
    "```",
    "",
    "## 한 줄씩 읽기",
    "```python",
    "with open(\"info.txt\", \"r\") as file:",
    "    for line in file:",
    "        name, weight, height = line.strip().split(\", \")",
    "        print(name, weight, height)",
    "```",
    "",
    "---",
    "### 요약",
    "- `lambda x: 식`: 익명 함수",
    "- `map(func, seq)`, `filter(func, seq)`",
    "- 튜플 `(a, b)`는 수정 불가",
    "- `with open(...) as f`로 파일 안전하게 열기"
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
