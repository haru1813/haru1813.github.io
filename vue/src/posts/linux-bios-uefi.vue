<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "linux-bios-uefi",
  title: "BIOS와 UEFI란? - 컴퓨터 부팅의 시작",
  date: "2026-02-24",
  summary: "BIOS와 UEFI는 컴퓨터 전원을 켰을 때 하드웨어를 초기화하고 OS를 불러오는 펌웨어입니다. BIOS에서 UEFI로의 전환, 부팅 흐름, Secure Boot, ESP까지 정리합니다.",
  category: "Technology/Linux",
  tags: ["linux","bios","uefi","boot","firmware","secure-boot","esp","basics"],
  content: [
    "## 한 줄 요약",
    "**BIOS/UEFI**는 컴퓨터 전원을 켜면 **가장 먼저 실행되는 펌웨어**로, 하드웨어를 초기화하고 운영체제를 불러오는 역할을 합니다. UEFI는 1980년대 BIOS를 대체하는 현대 표준입니다.",
    "",
    "## 펌웨어(Firmware)란?",
    "",
    "펌웨어는 하드웨어에 내장된 소프트웨어입니다. 메인보드의 ROM/플래시 메모리에 저장되어 있으며, 전원이 들어오면 CPU가 가장 먼저 이 펌웨어를 실행합니다.",
    "",
    "| 용어 | 설명 |",
    "|------|------|",
    "| 펌웨어 | 하드웨어에 내장된 소프트웨어 (BIOS/UEFI) |",
    "| 부트로더 | 펌웨어가 불러오는 OS 로딩 프로그램 (GRUB 등) |",
    "| 커널 | 부트로더가 메모리에 올리는 운영체제 핵심 |",
    "",
    "## BIOS (Basic Input/Output System)",
    "",
    "### 개요",
    "- **1981년** IBM PC와 함께 등장한 구형 펌웨어",
    "- 40년 넘게 사용되었으며, 현재는 레거시(Legacy) 방식으로 분류",
    "",
    "### 특징",
    "| 항목 | 내용 |",
    "|------|------|",
    "| 인터페이스 | 텍스트 기반, 키보드만 사용 |",
    "| 주소 모드 | 16비트 리얼 모드로 시작 |",
    "| 디스크 | MBR 파티션 테이블만 지원 (최대 2TB) |",
    "| 부팅 | 디스크 첫 512바이트(MBR)에서 부트로더를 찾음 |",
    "| 보안 | 부팅 보안 기능 없음 |",
    "| 드라이버 | 인터럽트 기반, 제한적 |",
    "",
    "### BIOS 부팅 흐름",
    "```",
    "전원 ON",
    "  → CPU가 BIOS 실행",
    "  → POST (Power-On Self-Test): 하드웨어 점검",
    "  → 부팅 장치 탐색 (HDD/SSD/USB 순서)",
    "  → 디스크의 MBR (첫 512바이트) 읽기",
    "  → MBR 안의 부트로더(GRUB 등) 실행",
    "  → 부트로더가 커널을 메모리에 로드",
    "  → 운영체제 시작",
    "```",
    "",
    "### POST (Power-On Self-Test)",
    "BIOS가 가장 먼저 수행하는 하드웨어 자가 진단입니다.",
    "",
    "| 점검 항목 | 내용 |",
    "|----------|------|",
    "| CPU | 프로세서 동작 확인 |",
    "| RAM | 메모리 존재·용량 확인 |",
    "| 키보드/마우스 | 입력 장치 연결 확인 |",
    "| 저장 장치 | HDD/SSD 인식 확인 |",
    "| 비디오 | 화면 출력 확인 |",
    "",
    "문제가 있으면 비프음(beep code)으로 알려줍니다.",
    "",
    "## UEFI (Unified Extensible Firmware Interface)",
    "",
    "### 개요",
    "- Intel이 **EFI**로 시작(1998), 이후 여러 기업이 합류해 **UEFI**로 표준화",
    "- 현재 판매되는 거의 모든 PC/노트북이 UEFI를 사용",
    "",
    "### 특징",
    "| 항목 | 내용 |",
    "|------|------|",
    "| 인터페이스 | GUI 가능, 마우스 지원 |",
    "| 주소 모드 | 32/64비트 모드로 동작 |",
    "| 디스크 | GPT 파티션 테이블 지원 (2TB 초과 가능, 최대 128개 파티션) |",
    "| 부팅 | ESP(EFI System Partition)에서 .efi 파일을 직접 실행 |",
    "| 보안 | Secure Boot 지원 |",
    "| 드라이버 | EFI 드라이버 모델, 네트워크 부팅 등 확장 가능 |",
    "| 부팅 속도 | BIOS보다 빠름 |",
    "",
    "### UEFI 부팅 흐름",
    "```",
    "전원 ON",
    "  → CPU가 UEFI 펌웨어 실행",
    "  → 하드웨어 초기화",
    "  → ESP(EFI System Partition) 탐색",
    "  → ESP 안의 .efi 부트로더 실행 (예: grubx64.efi)",
    "  → 부트로더가 커널을 메모리에 로드",
    "  → 운영체제 시작",
    "```",
    "",
    "## BIOS vs UEFI 비교",
    "",
    "| 기준 | BIOS | UEFI |",
    "|------|------|------|",
    "| 등장 | 1981년 | 2000년대~ |",
    "| 비트 모드 | 16비트 | 32/64비트 |",
    "| 파티션 테이블 | MBR (최대 2TB) | GPT (사실상 무제한) |",
    "| 최대 파티션 수 | 4개 (MBR 주 파티션) | 128개 (GPT) |",
    "| 부팅 방식 | MBR 첫 512바이트 | ESP의 .efi 파일 |",
    "| 부팅 속도 | 느림 | 빠름 |",
    "| 인터페이스 | 텍스트, 키보드 | GUI, 마우스 가능 |",
    "| 보안 부팅 | 없음 | Secure Boot |",
    "| 네트워크 부팅 | 제한적 | 기본 지원 |",
    "| 현재 상태 | 레거시 | 표준 |",
    "",
    "## ESP (EFI System Partition)",
    "",
    "UEFI 부팅에 필요한 **전용 파티션**입니다.",
    "",
    "| 항목 | 내용 |",
    "|------|------|",
    "| 파일시스템 | FAT32 (UEFI 표준 요구) |",
    "| 크기 | 보통 256MB~512MB |",
    "| 마운트 위치 | /boot/efi (리눅스) |",
    "| 내용물 | 부트로더 .efi 파일, 커널 이미지 등 |",
    "",
    "### ESP 내부 구조 예시",
    "```",
    "/boot/efi/",
    "  └─ EFI/",
    "       ├─ BOOT/",
    "       │   └─ BOOTX64.EFI      (기본 부트로더)",
    "       ├─ ubuntu/",
    "       │   └─ grubx64.efi      (Ubuntu GRUB)",
    "       └─ Microsoft/",
    "           └─ Boot/",
    "               └─ bootmgfw.efi (Windows 부트매니저)",
    "```",
    "",
    "멀티부팅 시 각 OS의 부트로더가 ESP 안에 폴더별로 존재합니다.",
    "",
    "## Secure Boot",
    "",
    "Secure Boot는 UEFI의 **보안 부팅 기능**입니다.",
    "",
    "| 항목 | 설명 |",
    "|------|------|",
    "| 목적 | 서명된(신뢰된) 부트로더·드라이버만 실행 허용 |",
    "| 작동 | 부팅 시 .efi 파일의 디지털 서명을 검증 |",
    "| 장점 | 루트킷·악성 부트로더 차단 |",
    "| 주의 | 서명되지 않은 OS/드라이버가 부팅 안 될 수 있음 |",
    "",
    "대부분의 주요 리눅스 배포판(Ubuntu, Fedora, RHEL 등)은 Secure Boot를 지원하므로, 켜둔 상태로 설치·부팅이 가능합니다. 일부 커스텀 커널이나 드라이버는 서명이 없어 Secure Boot를 끄거나 직접 서명해야 할 수 있습니다.",
    "",
    "## 부트로더란?",
    "",
    "부트로더(Boot Loader)는 **펌웨어(BIOS/UEFI)가 실행한 뒤, 운영체제 커널을 메모리에 올려주는 프로그램**입니다. 펌웨어는 하드웨어를 초기화하는 것까지만 담당하고, 실제로 \"어떤 OS를 어떻게 시작할지\"는 부트로더가 결정합니다.",
    "",
    "```",
    "펌웨어(BIOS/UEFI) → 부트로더(GRUB) → 커널(Linux) → OS 시작",
    "```",
    "",
    "| 부트로더 | OS | 설명 |",
    "|----------|-----|------|",
    "| GRUB | Linux | 가장 널리 쓰이는 리눅스 부트로더 |",
    "| systemd-boot | Linux | systemd 기반 경량 부트로더 |",
    "| Windows Boot Manager | Windows | bootmgfw.efi |",
    "| rEFInd | 멀티OS | GUI 멀티부팅 매니저 |",
    "",
    "## GRUB (GRand Unified Bootloader)",
    "",
    "**GRUB**는 리눅스에서 가장 많이 쓰이는 **표준 부트로더**입니다. 현재 사용되는 버전은 **GRUB 2**이며, 대부분의 리눅스 배포판이 기본으로 설치합니다.",
    "",
    "### GRUB가 하는 일",
    "",
    "| 역할 | 설명 |",
    "|------|------|",
    "| OS 선택 메뉴 | 멀티부팅 시 Linux / Windows 등 선택 화면 표시 |",
    "| 커널 로드 | 선택된 커널 이미지(vmlinuz)를 메모리에 올림 |",
    "| initramfs 로드 | 커널이 루트 파일시스템에 접근하기 위한 초기 RAM 디스크도 함께 로드 |",
    "| 커널 파라미터 전달 | root=, quiet, splash 등 부팅 옵션을 커널에 전달 |",
    "| 복구 모드 | 부팅 실패 시 GRUB 명령줄(셸)을 제공해 수동 복구 가능 |",
    "",
    "### GRUB 부팅 화면",
    "전원을 켜면 GRUB가 아래와 같은 메뉴를 보여줍니다.",
    "",
    "```",
    "┌──────────────────────────────────┐",
    "│  Ubuntu                          │",
    "│  Ubuntu (recovery mode)          │",
    "│  Advanced options for Ubuntu     │",
    "│  Windows Boot Manager            │",
    "│  UEFI Firmware Settings          │",
    "└──────────────────────────────────┘",
    "```",
    "",
    "- 화살표 키로 OS를 선택하고 Enter로 부팅",
    "- `e` 키를 누르면 커널 파라미터를 임시로 편집 가능 (트러블슈팅에 유용)",
    "- `c` 키를 누르면 GRUB 명령줄(셸)로 진입",
    "",
    "### GRUB의 동작 흐름",
    "```",
    "BIOS/UEFI가 GRUB 실행",
    "  → grub.cfg 읽기 (메뉴 항목, 커널 경로, 파라미터)",
    "  → OS 선택 메뉴 표시",
    "  → 사용자가 선택 (또는 타임아웃 시 기본 항목)",
    "  → 커널(vmlinuz) + initramfs를 메모리에 로드",
    "  → 커널에 제어권 넘김",
    "  → 커널이 하드웨어 감지 → 루트 파일시스템 마운트 → systemd 실행",
    "```",
    "",
    "### GRUB 설정 파일",
    "",
    "| 파일 | 역할 |",
    "|------|------|",
    "| /boot/grub/grub.cfg | GRUB 메뉴·부팅 설정 (자동 생성됨, **직접 수정 비권장**) |",
    "| /etc/default/grub | 사용자가 수정하는 설정 파일 (**이걸 수정**) |",
    "| /etc/grub.d/ | 메뉴 항목 생성 스크립트들 |",
    "",
    "### /etc/default/grub 주요 설정",
    "```bash",
    "GRUB_DEFAULT=0                    # 기본 선택 항목 (0번째)",
    "GRUB_TIMEOUT=5                    # 메뉴 대기 시간 (초)",
    "GRUB_CMDLINE_LINUX_DEFAULT=\"quiet splash\"  # 커널 파라미터",
    "GRUB_CMDLINE_LINUX=\"\"             # 복구 모드에도 적용할 파라미터",
    "GRUB_DISABLE_OS_PROBER=false      # 다른 OS 자동 감지 (멀티부팅)",
    "```",
    "",
    "| 설정 | 설명 |",
    "|------|------|",
    "| GRUB_DEFAULT | 기본 부팅 항목 번호 (0부터 시작) 또는 \"saved\" |",
    "| GRUB_TIMEOUT | 메뉴 표시 후 자동 부팅까지 대기 시간(초). 0이면 메뉴 생략 |",
    "| GRUB_CMDLINE_LINUX_DEFAULT | 일반 부팅 시 커널에 전달할 옵션 |",
    "| GRUB_DISABLE_OS_PROBER | false면 Windows 등 다른 OS를 자동 감지해 메뉴에 추가 |",
    "",
    "### 설정 변경 후 적용",
    "```bash",
    "# /etc/default/grub 수정 후 반드시 실행",
    "sudo update-grub                  # Ubuntu/Debian",
    "sudo grub2-mkconfig -o /boot/grub2/grub.cfg  # RHEL/CentOS",
    "```",
    "",
    "이 명령이 /etc/default/grub과 /etc/grub.d/ 스크립트를 읽어 grub.cfg를 새로 생성합니다.",
    "",
    "### GRUB 복구 (부팅이 안 될 때)",
    "GRUB가 깨져서 부팅이 안 되면, Live USB로 부팅 후 GRUB을 재설치할 수 있습니다.",
    "",
    "```bash",
    "# Live USB로 부팅 후",
    "sudo mount /dev/sda2 /mnt              # 루트 파티션 마운트",
    "sudo mount /dev/sda1 /mnt/boot/efi     # ESP 마운트 (UEFI인 경우)",
    "sudo grub-install --root-directory=/mnt /dev/sda",
    "sudo chroot /mnt update-grub",
    "```",
    "",
    "## 전체 부팅 순서 정리",
    "",
    "```",
    "[BIOS + MBR]",
    "전원 → BIOS → POST → MBR(512B) → GRUB → 커널 → init/systemd → 로그인",
    "",
    "[UEFI + GPT]",
    "전원 → UEFI → 하드웨어 초기화 → ESP(.efi) → GRUB → 커널 → init/systemd → 로그인",
    "```",
    "",
    "| 단계 | 담당 |",
    "|------|------|",
    "| 전원 ~ 하드웨어 초기화 | BIOS 또는 UEFI (펌웨어) |",
    "| 부트로더 실행 | GRUB 등 (MBR 또는 ESP에서 로드) |",
    "| 커널 로드 | 부트로더가 커널을 메모리에 올림 |",
    "| 시스템 초기화 | init 또는 systemd (PID 1) |",
    "| 서비스 시작 ~ 로그인 | systemd가 각종 서비스 실행 |",
    "",
    "## UEFI 설정 진입 방법",
    "",
    "| 제조사 | 키 |",
    "|--------|-----|",
    "| 대부분 | F2, Del |",
    "| HP | F10, Esc |",
    "| Lenovo | F1, F2 |",
    "| ASUS | F2, Del |",
    "| MSI | Del |",
    "",
    "부팅 직후 해당 키를 누르면 UEFI 설정 화면에 진입합니다. 여기서 부팅 순서, Secure Boot 켜기/끄기, 하드웨어 설정 등을 변경할 수 있습니다.",
    "",
    "---",
    "### 요약",
    "BIOS는 1981년부터 쓰인 구형 펌웨어(16비트, MBR, 2TB 제한)이고, UEFI는 이를 대체하는 현대 표준(32/64비트, GPT, Secure Boot)입니다. UEFI는 ESP(FAT32 파티션)에서 .efi 부트로더를 찾아 실행하며, GRUB이 커널을 메모리에 올려 운영체제를 시작합니다."
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
