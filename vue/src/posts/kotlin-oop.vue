<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "kotlin-oop",
  title: "Kotlin 객체 지향 프로그래밍 - 클래스, 상속, 가시성",
  date: "2026-03-03",
  summary: "Kotlin의 클래스 정의, 주/부 생성자, 상속(open), 오버라이딩, 내부 클래스, 가시성 제어자를 정리합니다.",
  category: "Coding/Kotlin",
  tags: ["kotlin", "클래스", "상속", "생성자", "가시성", "OOP"],
  content: [
    "## 클래스 정의",
    "Kotlin 클래스는 **프로퍼티**(속성)와 **메서드**(함수)로 구성됩니다.",
    "",
    "```kotlin",
    "class Bird {",
    "    var name: String = \"mybird\"",
    "    var wing: Int = 2",
    "    var beak: String = \"short\"",
    "    var color: String = \"blue\"",
    "",
    "    fun fly() = println(\"Fly wing: $wing\")",
    "    fun sing(vol: Int) = println(\"Sing vol: $vol\")",
    "}",
    "",
    "fun main() {",
    "    val coco = Bird()",
    "    coco.color = \"blue\"",
    "    println(coco.color)",
    "    coco.fly()",
    "    coco.sing(3)",
    "}",
    "```",
    "",
    "## 생성자",
    "### 주 생성자 (Primary Constructor)",
    "클래스 이름 옆에 선언합니다. `constructor` 키워드는 생략 가능합니다.",
    "",
    "```kotlin",
    "class Bird(var name: String, var wing: Int, var beak: String, var color: String) {",
    "    fun fly() = println(\"Fly wing: $wing\")",
    "    fun sing(vol: Int) = println(\"Sing vol: $vol\")",
    "}",
    "",
    "val coco = Bird(\"mybird\", 2, \"short\", \"blue\")",
    "```",
    "",
    "### init 블록",
    "```kotlin",
    "class Bird(var name: String, var wing: Int) {",
    "    init {",
    "        println(\"$name 새가 생성되었습니다.\")",
    "    }",
    "}",
    "```",
    "",
    "### 부 생성자 (Secondary Constructor)",
    "```kotlin",
    "class Bird {",
    "    var name: String",
    "    var wing: Int",
    "",
    "    constructor(name: String, wing: Int) {",
    "        this.name = name",
    "        this.wing = wing",
    "    }",
    "",
    "    constructor(name: String) : this(name, 2)",
    "}",
    "```",
    "",
    "## 상속",
    "Kotlin 클래스는 기본적으로 **final**입니다. 상속을 허용하려면 `open` 키워드가 필요합니다.",
    "",
    "```kotlin",
    "open class Bird(var name: String, var wing: Int, var beak: String, var color: String) {",
    "    fun fly() = println(\"Fly wing: $wing\")",
    "    fun sing(vol: Int) = println(\"Sing vol: $vol\")",
    "}",
    "",
    "class Lark(name: String, wing: Int, beak: String, color: String)",
    "    : Bird(name, wing, beak, color) {",
    "    fun singHitone() = println(\"Happy Song!\")",
    "}",
    "",
    "class Parrot : Bird {",
    "    val language: String",
    "",
    "    constructor(name: String, wing: Int, beak: String, color: String, language: String)",
    "        : super(name, wing, beak, color) {",
    "        this.language = language",
    "    }",
    "",
    "    fun speak() = println(\"Speak! $language\")",
    "}",
    "```",
    "",
    "### 오버라이딩 (override)",
    "```kotlin",
    "open class Bird {",
    "    open fun fly() = println(\"Bird flies\")",
    "}",
    "",
    "class Eagle : Bird() {",
    "    override fun fly() = println(\"Eagle soars high!\")",
    "}",
    "```",
    "",
    "## this와 super",
    "```kotlin",
    "open class Base {",
    "    open val x: Int = 1",
    "    open fun f() = println(\"Base Class f()\")",
    "}",
    "",
    "class Child : Base() {",
    "    override val x: Int = super.x + 1",
    "    override fun f() = println(\"Child Class f()\")",
    "",
    "    inner class Inside {",
    "        fun f() = println(\"Inside Class f()\")",
    "        fun test() {",
    "            f()              // Inside의 f()",
    "            Child().f()      // Child의 f()",
    "            super@Child.f()  // Base의 f()",
    "        }",
    "    }",
    "}",
    "```",
    "",
    "## 내부 클래스 vs 중첩 클래스",
    "| 종류 | 키워드 | 외부 클래스 접근 |",
    "|------|--------|------------------|",
    "| 중첩 클래스 (Nested) | 없음 | 불가 |",
    "| 내부 클래스 (Inner) | `inner` | 가능 (`this@Outer`) |",
    "",
    "```kotlin",
    "class Outer {",
    "    val outerVal = 10",
    "",
    "    class Nested {",
    "        fun hello() = println(\"Nested class\")",
    "    }",
    "",
    "    inner class Inner {",
    "        fun hello() = println(\"Inner class: $outerVal\")",
    "    }",
    "}",
    "",
    "Outer.Nested().hello()   // Nested class",
    "Outer().Inner().hello()  // Inner class: 10",
    "```",
    "",
    "## 가시성 제어자 (Visibility Modifiers)",
    "| 제어자 | 클래스 멤버 | 최상위 선언 |",
    "|--------|-------------|-------------|",
    "| `public` (기본) | 모든 곳에서 접근 | 모든 곳에서 접근 |",
    "| `private` | 같은 클래스 내부 | 같은 파일 내부 |",
    "| `protected` | 같은 클래스 + 하위 클래스 | 최상위에서 사용 불가 |",
    "| `internal` | 같은 모듈 | 같은 모듈 |",
    "",
    "```kotlin",
    "private class PrivateClass {",
    "    private var i = 1",
    "    private fun privateFunc() { i += 1 }",
    "    fun access() { privateFunc() }",
    "}",
    "",
    "fun main() {",
    "    val pc = PrivateClass()",
    "    // pc.i             // 오류! private",
    "    // pc.privateFunc() // 오류! private",
    "}",
    "```",
    "",
    "## 클래스 관계",
    "### 구성 (Composition)",
    "포함 관계이며 생명주기가 동일합니다.",
    "",
    "```kotlin",
    "class Car(val name: String, val power: String) {",
    "    private var engine = Engine(power)",
    "",
    "    fun startEngine() = engine.start()",
    "    fun stopEngine() = engine.stop()",
    "}",
    "",
    "class Engine(power: String) {",
    "    fun start() = println(\"Engine has been started.\")",
    "    fun stop() = println(\"Engine has been stopped.\")",
    "}",
    "```",
    "",
    "---",
    "### 요약",
    "- 클래스는 주 생성자(`var`/`val`)와 부 생성자(`constructor`)로 초기화",
    "- `open`으로 상속 허용, `override`로 재정의",
    "- `inner` 클래스는 외부 클래스 접근 가능, 중첩 클래스는 불가",
    "- 가시성: `public` > `internal` > `protected` > `private`"
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
