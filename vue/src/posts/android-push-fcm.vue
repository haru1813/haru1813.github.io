<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "android-push-fcm",
  title: "FCM 푸시 알림",
  date: "2025-12-11",
  summary: "Firebase Cloud Messaging(FCM)을 활용한 푸시 알림 구현 방법을 학습합니다. 토큰 관리, 메시지 수신, 서버 전송까지 실무에 필요한 내용을 다룹니다.",
  category: "Technology/Android",
  tags: ["android", "fcm", "push", "firebase", "cloudmessaging"],
  content: [
    "## 푸시 알림이란?",
    "",
    "푸시 알림은 서버에서 클라이언트(앱)으로 메시지를 전송하는 기술입니다. 사용자가 앱을 사용하고 있지 않더라도 중요한 정보를 실시간으로 전달할 수 있습니다.",
    "",
    "```mermaid",
    "sequenceDiagram",
    "    participant Server as 앱 서버",
    "    participant FCM as FCM 서버",
    "    participant Device as 안드로이드 기기",
    "    Device->>FCM: 1. 토큰 등록 요청",
    "    FCM-->>Device: 2. FCM 토큰 발급",
    "    Device->>Server: 3. 토큰 전송 (저장)",
    "    Note over Server: 이벤트 발생",
    "    Server->>FCM: 4. 메시지 전송 (토큰 포함)",
    "    FCM->>Device: 5. 푸시 알림 전달",
    "    Device->>Device: 6. 알림 표시",
    "```",
    "",
    "### 푸시 알림의 장점",
    "",
    "| 장점 | 설명 |",
    "|------|------|",
    "| 실시간 전달 | 서버 이벤트를 즉시 사용자에게 전달 |",
    "| 배터리 효율 | 폴링 방식보다 배터리 소모 적음 |",
    "| 중앙 관리 | FCM 서버가 전송/재시도/큐잉 관리 |",
    "| 크로스 플랫폼 | Android, iOS, Web 모두 지원 |",
    "| 무료 | FCM은 무료 서비스 |",
    "",
    "## FCM 아키텍처",
    "",
    "```mermaid",
    "graph TB",
    "    A[앱 서버] -->|HTTP v1 API| B[FCM 백엔드]",
    "    B --> C[전송 레이어]",
    "    C -->|Android| D[Google Play 서비스]",
    "    D --> E[안드로이드 앱]",
    "    C -->|iOS| F[APNs]",
    "    F --> G[iOS 앱]",
    "    C -->|Web| H[Web Push]",
    "    H --> I[웹 앱]",
    "    subgraph 메시지 타입",
    "        J[알림 메시지<br>Notification Message]",
    "        K[데이터 메시지<br>Data Message]",
    "    end",
    "```",
    "",
    "## Firebase 프로젝트 설정",
    "",
    "### 1단계: Firebase 프로젝트 생성",
    "",
    "Firebase Console(https://console.firebase.google.com)에서 새 프로젝트를 생성합니다.",
    "",
    "### 2단계: google-services.json 다운로드",
    "",
    "Firebase Console에서 안드로이드 앱을 등록하고 `google-services.json` 파일을 다운로드하여 `app/` 디렉토리에 배치합니다.",
    "",
    "### 3단계: build.gradle 의존성 설정",
    "",
    "```groovy",
    "// 프로젝트 수준 build.gradle",
    "buildscript {",
    "    dependencies {",
    "        classpath 'com.google.gms:google-services:4.4.0'",
    "    }",
    "}",
    "",
    "// 앱 수준 build.gradle",
    "plugins {",
    "    id 'com.android.application'",
    "    id 'com.google.gms.google-services'",
    "}",
    "",
    "dependencies {",
    "    implementation platform('com.google.firebase:firebase-bom:32.7.0')",
    "    implementation 'com.google.firebase:firebase-messaging'",
    "    implementation 'com.google.firebase:firebase-analytics'",
    "}",
    "```",
    "",
    "## FirebaseMessagingService 구현",
    "",
    "FCM 메시지를 수신하려면 `FirebaseMessagingService`를 상속하는 서비스를 구현합니다.",
    "",
    "```java",
    "public class MyFirebaseMessagingService extends FirebaseMessagingService {",
    "",
    "    @Override",
    "    public void onNewToken(@NonNull String token) {",
    "        super.onNewToken(token);",
    "        Log.d(\"FCM\", \"새 토큰: \" + token);",
    "        sendTokenToServer(token);",
    "    }",
    "",
    "    @Override",
    "    public void onMessageReceived(@NonNull RemoteMessage remoteMessage) {",
    "        super.onMessageReceived(remoteMessage);",
    "",
    "        Log.d(\"FCM\", \"메시지 수신 - From: \" + remoteMessage.getFrom());",
    "",
    "        // 알림 메시지 처리",
    "        if (remoteMessage.getNotification() != null) {",
    "            String title = remoteMessage.getNotification().getTitle();",
    "            String body = remoteMessage.getNotification().getBody();",
    "            Log.d(\"FCM\", \"알림 - 제목: \" + title + \", 내용: \" + body);",
    "            showNotification(title, body);",
    "        }",
    "",
    "        // 데이터 메시지 처리",
    "        if (remoteMessage.getData().size() > 0) {",
    "            Map<String, String> data = remoteMessage.getData();",
    "            Log.d(\"FCM\", \"데이터: \" + data);",
    "            handleDataMessage(data);",
    "        }",
    "    }",
    "",
    "    private void showNotification(String title, String body) {",
    "        Intent intent = new Intent(this, MainActivity.class);",
    "        intent.addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP);",
    "",
    "        PendingIntent pendingIntent = PendingIntent.getActivity(",
    "            this, 0, intent,",
    "            PendingIntent.FLAG_ONE_SHOT | PendingIntent.FLAG_IMMUTABLE);",
    "",
    "        NotificationCompat.Builder builder =",
    "            new NotificationCompat.Builder(this, \"fcm_channel\")",
    "                .setSmallIcon(R.drawable.ic_notification)",
    "                .setContentTitle(title)",
    "                .setContentText(body)",
    "                .setAutoCancel(true)",
    "                .setPriority(NotificationCompat.PRIORITY_HIGH)",
    "                .setContentIntent(pendingIntent);",
    "",
    "        NotificationManager manager =",
    "            (NotificationManager) getSystemService(NOTIFICATION_SERVICE);",
    "",
    "        // Android 8.0+ 채널 생성",
    "        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {",
    "            NotificationChannel channel = new NotificationChannel(",
    "                \"fcm_channel\",",
    "                \"FCM 알림\",",
    "                NotificationManager.IMPORTANCE_HIGH);",
    "            manager.createNotificationChannel(channel);",
    "        }",
    "",
    "        manager.notify((int) System.currentTimeMillis(), builder.build());",
    "    }",
    "",
    "    private void handleDataMessage(Map<String, String> data) {",
    "        String type = data.get(\"type\");",
    "        String content = data.get(\"content\");",
    "",
    "        if (\"chat\".equals(type)) {",
    "            showNotification(\"새 채팅\", content);",
    "        } else if (\"order\".equals(type)) {",
    "            showNotification(\"주문 상태\", content);",
    "        }",
    "    }",
    "",
    "    private void sendTokenToServer(String token) {",
    "        // 서버에 토큰 전송 (Retrofit, OkHttp 등 사용)",
    "        Log.d(\"FCM\", \"서버에 토큰 전송: \" + token);",
    "    }",
    "}",
    "```",
    "",
    "### AndroidManifest.xml에 서비스 등록",
    "",
    "```xml",
    "<service",
    "    android:name=\".MyFirebaseMessagingService\"",
    "    android:exported=\"false\">",
    "    <intent-filter>",
    "        <action android:name=\"com.google.firebase.MESSAGING_EVENT\" />",
    "    </intent-filter>",
    "</service>",
    "```",
    "",
    "## 토큰 관리",
    "",
    "FCM 토큰은 앱이 설치된 특정 기기를 식별하는 고유 문자열입니다.",
    "",
    "```mermaid",
    "flowchart TD",
    "    A[앱 설치/실행] --> B[FCM SDK 초기화]",
    "    B --> C{기존 토큰<br>있는가?}",
    "    C -->|없음| D[새 토큰 발급]",
    "    C -->|있음| E[기존 토큰 사용]",
    "    D --> F[onNewToken 호출]",
    "    F --> G[서버에 토큰 저장]",
    "    E --> H{토큰 갱신<br>필요?}",
    "    H -->|Yes| D",
    "    H -->|No| I[정상 동작]",
    "```",
    "",
    "### 현재 토큰 가져오기",
    "",
    "```java",
    "FirebaseMessaging.getInstance().getToken()",
    "    .addOnCompleteListener(task -> {",
    "        if (!task.isSuccessful()) {",
    "            Log.w(\"FCM\", \"토큰 가져오기 실패\", task.getException());",
    "            return;",
    "        }",
    "",
    "        String token = task.getResult();",
    "        Log.d(\"FCM\", \"현재 토큰: \" + token);",
    "",
    "        // SharedPreferences에 저장",
    "        SharedPreferences prefs = getSharedPreferences(\"fcm\", MODE_PRIVATE);",
    "        String savedToken = prefs.getString(\"token\", \"\");",
    "",
    "        if (!token.equals(savedToken)) {",
    "            prefs.edit().putString(\"token\", token).apply();",
    "            sendTokenToServer(token);",
    "        }",
    "    });",
    "```",
    "",
    "### 토큰 갱신 시나리오",
    "",
    "| 시나리오 | 설명 |",
    "|----------|------|",
    "| 앱 최초 설치 | 새 토큰 발급 |",
    "| 앱 데이터 삭제 | 토큰 재발급 |",
    "| 새 기기로 복원 | 새 토큰 발급 |",
    "| 앱 업데이트 | 토큰이 변경될 수 있음 |",
    "| FCM 서버 측 갱신 | 자동으로 onNewToken 호출 |",
    "",
    "## 알림 메시지 vs 데이터 메시지",
    "",
    "FCM 메시지는 두 가지 유형이 있으며, 앱의 상태(포그라운드/백그라운드)에 따라 처리 방식이 다릅니다.",
    "",
    "### 메시지 유형 비교",
    "",
    "| 비교 항목 | 알림 메시지 | 데이터 메시지 |",
    "|-----------|------------|-------------|",
    "| JSON 키 | `notification` | `data` |",
    "| 포그라운드 | onMessageReceived | onMessageReceived |",
    "| 백그라운드 | **시스템 트레이 자동 표시** | onMessageReceived |",
    "| 커스터마이징 | 제한적 | 완전히 자유 |",
    "| 사용 시기 | 간단한 알림 | 데이터 기반 처리 필요 시 |",
    "",
    "### 포그라운드/백그라운드 수신 동작",
    "",
    "```mermaid",
    "flowchart TD",
    "    A[FCM 메시지 수신] --> B{메시지 타입?}",
    "    B -->|알림 메시지| C{앱 상태?}",
    "    B -->|데이터 메시지| D[항상 onMessageReceived]",
    "    B -->|알림+데이터| E{앱 상태?}",
    "    C -->|포그라운드| F[onMessageReceived]",
    "    C -->|백그라운드| G[시스템 트레이<br>자동 표시]",
    "    E -->|포그라운드| H[onMessageReceived<br>알림+데이터 모두 접근]",
    "    E -->|백그라운드| I[시스템 트레이 표시<br>데이터는 Intent extras로 전달]",
    "```",
    "",
    "### JSON 메시지 구조 예시",
    "",
    "```json",
    "{",
    "  \"message\": {",
    "    \"token\": \"디바이스_FCM_토큰\",",
    "    \"notification\": {",
    "      \"title\": \"새 메시지\",",
    "      \"body\": \"홍길동님이 메시지를 보냈습니다.\"",
    "    },",
    "    \"data\": {",
    "      \"type\": \"chat\",",
    "      \"sender_id\": \"user123\",",
    "      \"message_id\": \"msg456\"",
    "    }",
    "  }",
    "}",
    "```",
    "",
    "## Firebase Console에서 테스트 메시지 보내기",
    "",
    "1. Firebase Console > Cloud Messaging 메뉴 진입",
    "2. **새 알림 만들기** 클릭",
    "3. 알림 제목과 내용 입력",
    "4. **테스트 메시지 보내기** 클릭",
    "5. FCM 토큰 입력 후 전송",
    "",
    "이 방법은 개발 중 빠르게 테스트할 때 유용합니다.",
    "",
    "## 서버에서 FCM API로 메시지 전송",
    "",
    "### HTTP v1 API (권장)",
    "",
    "```java",
    "// 서버 사이드 코드 (Java - Spring Boot 예시)",
    "// GoogleCredentials를 사용하여 인증",
    "",
    "public class FcmService {",
    "    private static final String FCM_URL =",
    "        \"https://fcm.googleapis.com/v1/projects/YOUR_PROJECT_ID/messages:send\";",
    "",
    "    public void sendMessage(String targetToken, String title, String body) {",
    "        String accessToken = getAccessToken();",
    "",
    "        JSONObject message = new JSONObject();",
    "        JSONObject msg = new JSONObject();",
    "        JSONObject notification = new JSONObject();",
    "",
    "        notification.put(\"title\", title);",
    "        notification.put(\"body\", body);",
    "",
    "        msg.put(\"token\", targetToken);",
    "        msg.put(\"notification\", notification);",
    "        message.put(\"message\", msg);",
    "",
    "        // HTTP POST 요청",
    "        OkHttpClient client = new OkHttpClient();",
    "        RequestBody requestBody = RequestBody.create(",
    "            message.toString(),",
    "            MediaType.parse(\"application/json\"));",
    "",
    "        Request request = new Request.Builder()",
    "            .url(FCM_URL)",
    "            .addHeader(\"Authorization\", \"Bearer \" + accessToken)",
    "            .addHeader(\"Content-Type\", \"application/json\")",
    "            .post(requestBody)",
    "            .build();",
    "",
    "        try (Response response = client.newCall(request).execute()) {",
    "            System.out.println(\"FCM 응답: \" + response.body().string());",
    "        }",
    "    }",
    "",
    "    private String getAccessToken() {",
    "        GoogleCredentials credentials = GoogleCredentials",
    "            .fromStream(new FileInputStream(\"service-account.json\"))",
    "            .createScoped(\"https://www.googleapis.com/auth/firebase.messaging\");",
    "        credentials.refreshIfExpired();",
    "        return credentials.getAccessToken().getTokenValue();",
    "    }",
    "}",
    "```",
    "",
    "## 토픽 구독 (Topic Messaging)",
    "",
    "토픽을 사용하면 특정 주제에 관심 있는 모든 기기에 메시지를 전송할 수 있습니다.",
    "",
    "```mermaid",
    "graph LR",
    "    A[서버] -->|토픽: news| B[FCM]",
    "    B --> C[구독 기기 1]",
    "    B --> D[구독 기기 2]",
    "    B --> E[구독 기기 3]",
    "    B -.->|비구독| F[기기 4]",
    "```",
    "",
    "### 토픽 구독/해제",
    "",
    "```java",
    "// 토픽 구독",
    "FirebaseMessaging.getInstance().subscribeToTopic(\"news\")",
    "    .addOnCompleteListener(task -> {",
    "        if (task.isSuccessful()) {",
    "            Log.d(\"FCM\", \"news 토픽 구독 성공\");",
    "        } else {",
    "            Log.e(\"FCM\", \"news 토픽 구독 실패\");",
    "        }",
    "    });",
    "",
    "// 토픽 해제",
    "FirebaseMessaging.getInstance().unsubscribeFromTopic(\"news\")",
    "    .addOnCompleteListener(task -> {",
    "        Log.d(\"FCM\", \"news 토픽 구독 해제\");",
    "    });",
    "```",
    "",
    "### 서버에서 토픽으로 메시지 전송",
    "",
    "```json",
    "{",
    "  \"message\": {",
    "    \"topic\": \"news\",",
    "    \"notification\": {",
    "      \"title\": \"속보\",",
    "      \"body\": \"새로운 뉴스가 도착했습니다.\"",
    "    },",
    "    \"data\": {",
    "      \"article_id\": \"12345\",",
    "      \"category\": \"technology\"",
    "    }",
    "  }",
    "}",
    "```",
    "",
    "## 조건부 전송",
    "",
    "여러 토픽을 조합하여 조건부로 메시지를 전송할 수 있습니다.",
    "",
    "```json",
    "{",
    "  \"message\": {",
    "    \"condition\": \"'news' in topics && 'technology' in topics\",",
    "    \"notification\": {",
    "      \"title\": \"IT 뉴스\",",
    "      \"body\": \"기술 관련 새 소식이 있습니다.\"",
    "    }",
    "  }",
    "}",
    "```",
    "",
    "### 조건 연산자",
    "",
    "| 연산자 | 설명 | 예시 |",
    "|--------|------|------|",
    "| `&&` | AND | `'A' in topics && 'B' in topics` |",
    "| `\\|\\|` | OR | `'A' in topics \\|\\| 'B' in topics` |",
    "| `!` | NOT | `!('A' in topics)` |",
    "",
    "## FCM 주의사항과 제한",
    "",
    "| 항목 | 제한 |",
    "|------|------|",
    "| 메시지 크기 | 알림: 2KB, 데이터: 4KB |",
    "| 토픽 구독 수 | 앱당 2,000개 토픽 |",
    "| 전송 속도 | 초당 약 500개 메시지 |",
    "| 토큰 수명 | 갱신 필요 (보통 수개월) |",
    "| 우선순위 | HIGH vs NORMAL (배터리 관리) |",
    "",
    "### 실무 팁",
    "",
    "```mermaid",
    "flowchart TD",
    "    A[FCM 실무 팁] --> B[토큰 관리]",
    "    A --> C[메시지 전략]",
    "    A --> D[에러 처리]",
    "    B --> B1[토큰 갱신 시<br>서버 동기화]",
    "    B --> B2[만료 토큰<br>서버에서 제거]",
    "    C --> C1[데이터 메시지 우선<br>백그라운드 커스텀 처리]",
    "    C --> C2[토픽 vs 개별 전송<br>상황에 맞게 선택]",
    "    D --> D1[전송 실패 시<br>재시도 로직]",
    "    D --> D2[InvalidRegistration<br>토큰 제거]",
    "```",
    "",
    "### Android 13+ 알림 권한",
    "",
    "Android 13(API 33)부터 알림을 표시하려면 `POST_NOTIFICATIONS` 권한을 런타임에 요청해야 합니다.",
    "",
    "```java",
    "// Android 13+ 알림 권한 요청",
    "private final ActivityResultLauncher<String> notificationPermissionLauncher =",
    "    registerForActivityResult(",
    "        new ActivityResultContracts.RequestPermission(),",
    "        isGranted -> {",
    "            if (isGranted) {",
    "                Log.d(\"FCM\", \"알림 권한 허용됨\");",
    "            } else {",
    "                Log.d(\"FCM\", \"알림 권한 거부됨\");",
    "            }",
    "        }",
    "    );",
    "",
    "private void checkNotificationPermission() {",
    "    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {",
    "        if (ContextCompat.checkSelfPermission(this,",
    "                Manifest.permission.POST_NOTIFICATIONS)",
    "                != PackageManager.PERMISSION_GRANTED) {",
    "            notificationPermissionLauncher.launch(",
    "                Manifest.permission.POST_NOTIFICATIONS);",
    "        }",
    "    }",
    "}",
    "```",
    "",
    "## 정리",
    "",
    "FCM은 안드로이드 앱에서 푸시 알림을 구현하는 가장 표준적인 방법입니다. FirebaseMessagingService를 통해 메시지를 수신하고, 토큰 관리를 올바르게 처리하며, 알림 메시지와 데이터 메시지의 차이를 이해하는 것이 중요합니다. 토픽 구독을 활용하면 효율적으로 그룹 메시지를 전송할 수 있으며, HTTP v1 API를 통해 서버에서 프로그래매틱하게 메시지를 전송할 수 있습니다."
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
