<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "android-permission",
  title: "권한(Permission) 처리",
  date: "2025-12-18",
  summary: "Android 권한 시스템의 개념부터 런타임 권한 요청, 거부 처리, ActivityResultLauncher 사용법까지 실전 코드와 함께 정리합니다.",
  category: "Technology/Android",
  tags: ["android", "permission", "runtime", "dangerous", "manifest"],
  content: [
    "## 권한이란 (보안 모델)",
    "",
    "Android는 **샌드박스(Sandbox) 기반** 보안 모델을 사용합니다. 각 앱은 고유한 Linux 사용자 ID를 부여받으며, 기본적으로 다른 앱이나 시스템 리소스에 접근할 수 없습니다.",
    "",
    "앱이 민감한 데이터나 기능에 접근하려면 **권한(Permission)**을 선언하고 사용자 동의를 받아야 합니다.",
    "",
    "```mermaid",
    "flowchart TD",
    "    A[앱 설치] --> B[AndroidManifest.xml 권한 확인]",
    "    B --> C{권한 종류}",
    "    C -->|일반 권한| D[자동 부여]",
    "    C -->|위험 권한| E[런타임 권한 요청 필요]",
    "    C -->|서명 권한| F[같은 서명의 앱만 사용 가능]",
    "```",
    "",
    "## 일반 권한 vs 위험 권한",
    "",
    "Android는 권한을 **보호 수준(Protection Level)**에 따라 분류합니다.",
    "",
    "### 일반 권한 (Normal Permission)",
    "",
    "사용자의 프라이버시에 직접적인 위험이 없는 권한입니다. 매니페스트에 선언만 하면 **설치 시 자동 부여**됩니다.",
    "",
    "| 권한 | 설명 |",
    "| --- | --- |",
    "| INTERNET | 인터넷 접근 |",
    "| ACCESS_NETWORK_STATE | 네트워크 상태 확인 |",
    "| ACCESS_WIFI_STATE | Wi-Fi 상태 확인 |",
    "| SET_ALARM | 알람 설정 |",
    "| VIBRATE | 진동 사용 |",
    "| WAKE_LOCK | 화면 꺼짐 방지 |",
    "| RECEIVE_BOOT_COMPLETED | 부팅 완료 수신 |",
    "",
    "### 위험 권한 (Dangerous Permission)",
    "",
    "사용자의 개인정보나 민감한 데이터에 접근하는 권한입니다. Android 6.0(API 23) 이상에서는 **런타임에 사용자 동의**를 받아야 합니다.",
    "",
    "| 권한 그룹 | 포함 권한 | 설명 |",
    "| --- | --- | --- |",
    "| CAMERA | CAMERA | 카메라 사용 |",
    "| LOCATION | ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION | 위치 정보 |",
    "| STORAGE | READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE | 외부 저장소 접근 |",
    "| CONTACTS | READ_CONTACTS, WRITE_CONTACTS, GET_ACCOUNTS | 연락처 접근 |",
    "| PHONE | READ_PHONE_STATE, CALL_PHONE, READ_CALL_LOG | 전화 관련 |",
    "| MICROPHONE | RECORD_AUDIO | 마이크 녹음 |",
    "| SMS | SEND_SMS, RECEIVE_SMS, READ_SMS | 문자 관련 |",
    "| CALENDAR | READ_CALENDAR, WRITE_CALENDAR | 캘린더 접근 |",
    "| SENSORS | BODY_SENSORS | 신체 센서(심박수 등) |",
    "",
    "## AndroidManifest.xml에 권한 선언",
    "",
    "모든 권한은 먼저 매니페스트에 선언해야 합니다.",
    "",
    "```xml",
    "<manifest xmlns:android=\"http://schemas.android.com/apk/res/android\"",
    "    package=\"com.example.permissionapp\">",
    "",
    "    <!-- 일반 권한 -->",
    "    <uses-permission android:name=\"android.permission.INTERNET\" />",
    "",
    "    <!-- 위험 권한 -->",
    "    <uses-permission android:name=\"android.permission.CAMERA\" />",
    "    <uses-permission android:name=\"android.permission.ACCESS_FINE_LOCATION\" />",
    "    <uses-permission android:name=\"android.permission.READ_CONTACTS\" />",
    "    <uses-permission android:name=\"android.permission.RECORD_AUDIO\" />",
    "",
    "    <!-- 하드웨어 기능 선언 (선택) -->",
    "    <uses-feature",
    "        android:name=\"android.hardware.camera\"",
    "        android:required=\"false\" />",
    "",
    "    <application ... >",
    "    </application>",
    "</manifest>",
    "```",
    "",
    "`uses-feature`에서 `required=\"false\"`를 설정하면 해당 하드웨어가 없는 기기에서도 앱을 설치할 수 있습니다.",
    "",
    "## 런타임 권한 요청 흐름",
    "",
    "Android 6.0(API 23) 이상에서 위험 권한을 사용하려면 런타임에 사용자 동의를 받아야 합니다.",
    "",
    "```mermaid",
    "flowchart TD",
    "    A[권한이 필요한 기능 실행] --> B{권한 확인<br/>checkSelfPermission}",
    "    B -->|이미 허용됨| C[기능 바로 실행]",
    "    B -->|허용되지 않음| D{이전에 거부한 적 있음?<br/>shouldShowRequestPermissionRationale}",
    "    D -->|Yes| E[권한 필요 이유 설명 다이얼로그]",
    "    D -->|No| F[권한 요청 다이얼로그 표시]",
    "    E --> F",
    "    F --> G{사용자 응답}",
    "    G -->|허용| H[기능 실행]",
    "    G -->|거부| I{다시 묻지 않기 체크?}",
    "    I -->|No| J[다음 요청 시 다시 물어봄]",
    "    I -->|Yes| K[설정 화면으로 안내]",
    "```",
    "",
    "## ActivityResultLauncher를 이용한 권한 요청",
    "",
    "기존 `requestPermissions()` + `onRequestPermissionsResult()` 방식 대신, 최신 API인 **ActivityResultLauncher**를 사용하는 것이 권장됩니다.",
    "",
    "### 단일 권한 요청",
    "",
    "```java",
    "public class CameraActivity extends AppCompatActivity {",
    "",
    "    private ActivityResultLauncher<String> cameraPermissionLauncher;",
    "",
    "    @Override",
    "    protected void onCreate(Bundle savedInstanceState) {",
    "        super.onCreate(savedInstanceState);",
    "        setContentView(R.layout.activity_camera);",
    "",
    "        cameraPermissionLauncher = registerForActivityResult(",
    "            new ActivityResultContracts.RequestPermission(),",
    "            isGranted -> {",
    "                if (isGranted) {",
    "                    openCamera();",
    "                } else {",
    "                    handlePermissionDenied();",
    "                }",
    "            }",
    "        );",
    "",
    "        findViewById(R.id.cameraButton).setOnClickListener(v -> {",
    "            checkAndRequestCameraPermission();",
    "        });",
    "    }",
    "",
    "    private void checkAndRequestCameraPermission() {",
    "        if (ContextCompat.checkSelfPermission(this, Manifest.permission.CAMERA)",
    "                == PackageManager.PERMISSION_GRANTED) {",
    "            openCamera();",
    "        } else if (shouldShowRequestPermissionRationale(",
    "                Manifest.permission.CAMERA)) {",
    "            showRationaleDialog();",
    "        } else {",
    "            cameraPermissionLauncher.launch(Manifest.permission.CAMERA);",
    "        }",
    "    }",
    "",
    "    private void showRationaleDialog() {",
    "        new AlertDialog.Builder(this)",
    "            .setTitle(\"카메라 권한 필요\")",
    "            .setMessage(\"사진을 촬영하려면 카메라 권한이 필요합니다.\")",
    "            .setPositiveButton(\"허용\", (dialog, which) -> {",
    "                cameraPermissionLauncher.launch(Manifest.permission.CAMERA);",
    "            })",
    "            .setNegativeButton(\"취소\", null)",
    "            .show();",
    "    }",
    "",
    "    private void openCamera() {",
    "        Toast.makeText(this, \"카메라를 실행합니다\", Toast.LENGTH_SHORT).show();",
    "    }",
    "",
    "    private void handlePermissionDenied() {",
    "        Toast.makeText(this, \"카메라 권한이 거부되었습니다\",",
    "            Toast.LENGTH_SHORT).show();",
    "    }",
    "}",
    "```",
    "",
    "## 다중 권한 요청",
    "",
    "여러 권한을 한 번에 요청할 수 있습니다.",
    "",
    "```java",
    "public class MapActivity extends AppCompatActivity {",
    "",
    "    private ActivityResultLauncher<String[]> multiPermissionLauncher;",
    "",
    "    private static final String[] REQUIRED_PERMISSIONS = {",
    "        Manifest.permission.ACCESS_FINE_LOCATION,",
    "        Manifest.permission.ACCESS_COARSE_LOCATION,",
    "        Manifest.permission.CAMERA",
    "    };",
    "",
    "    @Override",
    "    protected void onCreate(Bundle savedInstanceState) {",
    "        super.onCreate(savedInstanceState);",
    "        setContentView(R.layout.activity_map);",
    "",
    "        multiPermissionLauncher = registerForActivityResult(",
    "            new ActivityResultContracts.RequestMultiplePermissions(),",
    "            result -> {",
    "                boolean allGranted = true;",
    "                for (Map.Entry<String, Boolean> entry : result.entrySet()) {",
    "                    if (!entry.getValue()) {",
    "                        allGranted = false;",
    "                        Log.d(\"Permission\", entry.getKey() + \" 거부됨\");",
    "                    }",
    "                }",
    "                if (allGranted) {",
    "                    startMapFeature();",
    "                } else {",
    "                    showPermissionDeniedMessage();",
    "                }",
    "            }",
    "        );",
    "    }",
    "",
    "    private void checkPermissions() {",
    "        List<String> deniedPermissions = new ArrayList<>();",
    "        for (String permission : REQUIRED_PERMISSIONS) {",
    "            if (ContextCompat.checkSelfPermission(this, permission)",
    "                    != PackageManager.PERMISSION_GRANTED) {",
    "                deniedPermissions.add(permission);",
    "            }",
    "        }",
    "",
    "        if (deniedPermissions.isEmpty()) {",
    "            startMapFeature();",
    "        } else {",
    "            multiPermissionLauncher.launch(",
    "                deniedPermissions.toArray(new String[0]));",
    "        }",
    "    }",
    "}",
    "```",
    "",
    "## shouldShowRequestPermissionRationale",
    "",
    "이 메서드는 사용자가 이전에 권한을 거부한 적이 있는지 확인하는 데 사용됩니다.",
    "",
    "| 상황 | 반환값 | 설명 |",
    "| --- | --- | --- |",
    "| 처음 요청 | false | 아직 거부한 적 없음 |",
    "| 한 번 거부 | true | 이유 설명 후 재요청 권장 |",
    "| \"다시 묻지 않기\" 체크 후 거부 | false | 더 이상 시스템 다이얼로그 표시 불가 |",
    "",
    "```java",
    "private void handlePermissionRequest(String permission) {",
    "    if (ContextCompat.checkSelfPermission(this, permission)",
    "            == PackageManager.PERMISSION_GRANTED) {",
    "        // 이미 허용됨",
    "        executeFeature();",
    "    } else if (shouldShowRequestPermissionRationale(permission)) {",
    "        // 이전에 거부됨 - 왜 권한이 필요한지 설명",
    "        new AlertDialog.Builder(this)",
    "            .setTitle(\"권한 안내\")",
    "            .setMessage(\"이 기능을 사용하려면 해당 권한이 반드시 필요합니다.\")",
    "            .setPositiveButton(\"권한 요청\", (d, w) -> {",
    "                permissionLauncher.launch(permission);",
    "            })",
    "            .setNegativeButton(\"취소\", null)",
    "            .show();",
    "    } else {",
    "        // 처음 요청 또는 \"다시 묻지 않기\" 상태",
    "        permissionLauncher.launch(permission);",
    "    }",
    "}",
    "```",
    "",
    "## 권한 거부 시 처리 (설정 화면으로 안내)",
    "",
    "사용자가 \"다시 묻지 않기\"를 선택한 경우, 시스템 다이얼로그를 더 이상 표시할 수 없습니다. 이때는 **앱 설정 화면**으로 안내해야 합니다.",
    "",
    "```java",
    "private void goToAppSettings() {",
    "    new AlertDialog.Builder(this)",
    "        .setTitle(\"권한이 필요합니다\")",
    "        .setMessage(\"설정에서 직접 권한을 허용해 주세요.\")",
    "        .setPositiveButton(\"설정으로 이동\", (dialog, which) -> {",
    "            Intent intent = new Intent(",
    "                Settings.ACTION_APPLICATION_DETAILS_SETTINGS);",
    "            Uri uri = Uri.fromParts(\"package\", getPackageName(), null);",
    "            intent.setData(uri);",
    "            startActivity(intent);",
    "        })",
    "        .setNegativeButton(\"취소\", null)",
    "        .show();",
    "}",
    "```",
    "",
    "### 설정에서 돌아온 후 권한 재확인",
    "",
    "```java",
    "private ActivityResultLauncher<Intent> settingsLauncher;",
    "",
    "@Override",
    "protected void onCreate(Bundle savedInstanceState) {",
    "    super.onCreate(savedInstanceState);",
    "",
    "    settingsLauncher = registerForActivityResult(",
    "        new ActivityResultContracts.StartActivityForResult(),",
    "        result -> {",
    "            if (ContextCompat.checkSelfPermission(this,",
    "                    Manifest.permission.CAMERA)",
    "                    == PackageManager.PERMISSION_GRANTED) {",
    "                openCamera();",
    "            } else {",
    "                Toast.makeText(this, \"권한이 여전히 거부되어 있습니다\",",
    "                    Toast.LENGTH_SHORT).show();",
    "            }",
    "        }",
    "    );",
    "}",
    "```",
    "",
    "## 권한 그룹",
    "",
    "Android는 관련 권한을 **그룹**으로 묶어 관리합니다. 같은 그룹 내에서 하나의 권한이 허용되면, 해당 그룹의 다른 권한도 자동으로 허용될 수 있습니다. 다만 Android 버전에 따라 동작이 다를 수 있으므로, **각 권한을 개별적으로 확인하는 것이 안전**합니다.",
    "",
    "```mermaid",
    "graph TD",
    "    A[LOCATION 그룹] --> B[ACCESS_FINE_LOCATION]",
    "    A --> C[ACCESS_COARSE_LOCATION]",
    "    D[STORAGE 그룹] --> E[READ_EXTERNAL_STORAGE]",
    "    D --> F[WRITE_EXTERNAL_STORAGE]",
    "    G[CAMERA 그룹] --> H[CAMERA]",
    "    I[CONTACTS 그룹] --> J[READ_CONTACTS]",
    "    I --> K[WRITE_CONTACTS]",
    "    I --> L[GET_ACCOUNTS]",
    "```",
    "",
    "## targetSdkVersion에 따른 동작 차이",
    "",
    "| targetSdkVersion | 동작 |",
    "| --- | --- |",
    "| 22 이하 | 설치 시 모든 권한 자동 부여 (런타임 요청 불필요) |",
    "| 23~28 | 위험 권한은 런타임 요청 필요 |",
    "| 29 | 위치 권한에 ACCESS_BACKGROUND_LOCATION 추가 |",
    "| 30 | 저장소 접근에 Scoped Storage 적용 |",
    "| 31 | 블루투스 권한 세분화 (BLUETOOTH_SCAN, BLUETOOTH_CONNECT 등) |",
    "| 33 | 알림 권한 POST_NOTIFICATIONS 추가 |",
    "| 34 | 사진/동영상 부분 접근 권한 추가 |",
    "",
    "```java",
    "// Android 13(API 33) 이상 알림 권한 요청 예시",
    "if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {",
    "    if (ContextCompat.checkSelfPermission(this,",
    "            Manifest.permission.POST_NOTIFICATIONS)",
    "            != PackageManager.PERMISSION_GRANTED) {",
    "        notificationPermissionLauncher.launch(",
    "            Manifest.permission.POST_NOTIFICATIONS);",
    "    }",
    "}",
    "```",
    "",
    "## 권한 목록 정리 테이블",
    "",
    "| 권한 | 보호 수준 | 용도 | 런타임 요청 |",
    "| --- | --- | --- | --- |",
    "| INTERNET | 일반 | 네트워크 접근 | 불필요 |",
    "| CAMERA | 위험 | 카메라 사용 | 필요 |",
    "| ACCESS_FINE_LOCATION | 위험 | 정밀 위치(GPS) | 필요 |",
    "| ACCESS_COARSE_LOCATION | 위험 | 대략적 위치(Wi-Fi/셀) | 필요 |",
    "| ACCESS_BACKGROUND_LOCATION | 위험 | 백그라운드 위치 | 별도 요청 필요 |",
    "| READ_EXTERNAL_STORAGE | 위험 | 외부 저장소 읽기 | 필요 |",
    "| WRITE_EXTERNAL_STORAGE | 위험 | 외부 저장소 쓰기 | 필요(API 28 이하) |",
    "| READ_CONTACTS | 위험 | 연락처 읽기 | 필요 |",
    "| WRITE_CONTACTS | 위험 | 연락처 수정 | 필요 |",
    "| CALL_PHONE | 위험 | 전화 걸기 | 필요 |",
    "| READ_PHONE_STATE | 위험 | 전화 상태 읽기 | 필요 |",
    "| RECORD_AUDIO | 위험 | 마이크 녹음 | 필요 |",
    "| SEND_SMS | 위험 | 문자 전송 | 필요 |",
    "| READ_CALENDAR | 위험 | 캘린더 읽기 | 필요 |",
    "| POST_NOTIFICATIONS | 위험 | 알림 표시(API 33+) | 필요 |",
    "| VIBRATE | 일반 | 진동 | 불필요 |",
    "| WAKE_LOCK | 일반 | 화면 꺼짐 방지 | 불필요 |",
    "",
    "권한 처리는 사용자 경험에 직접적인 영향을 미칩니다. **필요한 시점에만 요청**하고, **왜 필요한지 충분히 설명**하며, **거부 시에도 앱이 정상적으로 동작**하도록 설계하는 것이 중요합니다."
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
