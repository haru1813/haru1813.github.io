<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "git-checkout",
  title: "Git checkout 명령어 완벽 가이드",
  date: "2026-02-25",
  summary: "git checkout의 브랜치 전환, 파일 복원, 과거 커밋 이동, 그리고 최신 대체 명령어 git switch / git restore까지 정리합니다.",
  category: "Technology/Git",
  tags: ["git","checkout","branch","switch","restore","detached-head"],
  content: [
    "## 한 줄 요약",
    "`git checkout`은 **브랜치 전환**과 **파일 복원** 두 가지 역할을 하며, Git 2.23부터는 `git switch`(브랜치)와 `git restore`(파일)로 분리되었습니다.",
    "",
    "---",
    "",
    "## checkout이 하는 일 (2가지)",
    "",
    "```mermaid",
    "flowchart LR",
    "    A[\"git checkout\"] --> B[\"1. 브랜치 전환 → git switch (신규 명령어)\"]",
    "    A --> C[\"2. 파일 복원 → git restore (신규 명령어)\"]",
    "```",
    "",
    "> `checkout`은 두 가지 전혀 다른 동작을 한 명령어로 처리해서 혼란스러웠기 때문에, Git 2.23(2019)에서 `switch`와 `restore`로 분리되었습니다. 기존 `checkout`도 여전히 사용 가능합니다.",
    "",
    "---",
    "",
    "## 1. 브랜치 전환",
    "",
    "### 기존 브랜치로 이동",
    "```bash",
    "# checkout 방식",
    "git checkout main",
    "git checkout develop",
    "git checkout feature/login",
    "",
    "# switch 방식 (권장)",
    "git switch main",
    "git switch develop",
    "```",
    "",
    "### 새 브랜치 생성 + 이동",
    "```bash",
    "# checkout 방식",
    "git checkout -b feature/signup",
    "",
    "# switch 방식 (권장)",
    "git switch -c feature/signup",
    "```",
    "",
    "| 옵션 | checkout | switch | 설명 |",
    "|------|----------|--------|------|",
    "| 브랜치 이동 | `git checkout 브랜치` | `git switch 브랜치` | 기존 브랜치로 전환 |",
    "| 새 브랜치 | `git checkout -b 이름` | `git switch -c 이름` | 생성과 동시에 이동 |",
    "| 이전 브랜치 | `git checkout -` | `git switch -` | 직전 브랜치로 돌아가기 |",
    "",
    "### 예시: 브랜치 작업 흐름",
    "```bash",
    "# 현재 main 브랜치",
    "git switch -c feature/login    # 새 브랜치 생성 + 이동",
    "",
    "# ... 코드 작업 ...",
    "git add .",
    "git commit -m \"로그인 기능 추가\"",
    "",
    "git switch main                # main으로 돌아가기",
    "git merge feature/login        # 병합",
    "git branch -d feature/login    # 브랜치 삭제",
    "```",
    "",
    "---",
    "",
    "## 2. 파일 복원",
    "",
    "### 수정한 파일을 되돌리기 (unstaged 변경 취소)",
    "```bash",
    "# checkout 방식",
    "git checkout -- index.html",
    "git checkout -- src/App.jsx",
    "git checkout -- .              # 모든 수정 파일 되돌리기",
    "",
    "# restore 방식 (권장)",
    "git restore index.html",
    "git restore src/App.jsx",
    "git restore .                  # 모든 수정 파일 되돌리기",
    "```",
    "",
    "> 이 명령은 **아직 `git add`하지 않은 수정 사항**을 마지막 커밋 상태로 되돌립니다. 되돌린 변경은 복구할 수 없으니 주의하세요.",
    "",
    "### 스테이지에서 내리기 (add 취소)",
    "```bash",
    "# 기존 방식",
    "git reset HEAD index.html",
    "",
    "# restore 방식 (권장)",
    "git restore --staged index.html",
    "git restore --staged .         # 모든 파일 unstage",
    "```",
    "",
    "### 특정 커밋의 파일 가져오기",
    "```bash",
    "# 3번 전 커밋의 파일 상태를 가져오기",
    "git checkout HEAD~3 -- src/App.jsx",
    "",
    "# restore 방식",
    "git restore --source HEAD~3 src/App.jsx",
    "",
    "# 특정 커밋 해시로",
    "git restore --source abc1234 src/App.jsx",
    "```",
    "",
    "| 동작 | checkout | restore | 설명 |",
    "|------|----------|---------|------|",
    "| 수정 취소 | `git checkout -- 파일` | `git restore 파일` | unstaged 변경 되돌리기 |",
    "| add 취소 | `git reset HEAD 파일` | `git restore --staged 파일` | 스테이지에서 내리기 |",
    "| 특정 커밋 복원 | `git checkout 커밋 -- 파일` | `git restore --source 커밋 파일` | 과거 파일 가져오기 |",
    "| 전체 되돌리기 | `git checkout -- .` | `git restore .` | 모든 수정 취소 |",
    "",
    "---",
    "",
    "## 3. 과거 커밋으로 이동 (Detached HEAD)",
    "",
    "특정 커밋을 확인하고 싶을 때 커밋 해시로 checkout합니다.",
    "",
    "```bash",
    "# 특정 커밋으로 이동",
    "git checkout abc1234",
    "",
    "# 태그로 이동",
    "git checkout v1.0.0",
    "",
    "# 3번 전 커밋으로",
    "git checkout HEAD~3",
    "```",
    "",
    "### Detached HEAD 상태란?",
    "",
    "```mermaid",
    "flowchart LR",
    "    subgraph s1[\"정상 상태 (HEAD → 브랜치 → 커밋)\"]",
    "        direction LR",
    "        A1[A] --> B1[B] --> C1[C] --> D1[D]",
    "        HEAD1([\"HEAD\"]) -.-> main1([\"main\"]) -.-> D1",
    "    end",
    "    subgraph s2[\"Detached HEAD (HEAD가 직접 커밋을 가리킴)\"]",
    "        direction LR",
    "        A2[A] --> B2[B] --> C2[C] --> D2[D]",
    "        HEAD2([\"HEAD\"]) -.-> B2",
    "        main2([\"main\"]) -.-> D2",
    "    end",
    "```",
    "",
    "| 상황 | 설명 |",
    "|------|------|",
    "| Detached HEAD에서 **구경만** | 문제 없음, `git switch main`으로 돌아오면 됨 |",
    "| Detached HEAD에서 **커밋** | 브랜치가 없으므로 나중에 잃어버릴 수 있음 |",
    "| Detached HEAD에서 **커밋 유지하려면** | `git switch -c 새브랜치`로 브랜치 생성 |",
    "",
    "```bash",
    "# 과거 커밋 확인",
    "git checkout abc1234",
    "",
    "# (경고 메시지: You are in 'detached HEAD' state...)",
    "",
    "# 구경만 하고 돌아가기",
    "git switch main",
    "",
    "# 또는 이 상태에서 새 브랜치로 작업하기",
    "git switch -c hotfix/old-bug",
    "```",
    "",
    "---",
    "",
    "## 4. 원격 브랜치 체크아웃",
    "",
    "```bash",
    "# 원격 브랜치 목록 확인",
    "git branch -r",
    "",
    "# 원격 브랜치를 로컬에 가져오면서 전환",
    "git checkout -b feature/api origin/feature/api",
    "",
    "# switch 방식 (자동으로 원격 추적)",
    "git switch feature/api",
    "",
    "# 먼저 원격 정보 업데이트 후",
    "git fetch origin",
    "git switch feature/api",
    "```",
    "",
    "> `git switch`는 로컬에 없는 브랜치명이 원격에 하나만 있으면 자동으로 추적 브랜치를 생성합니다.",
    "",
    "---",
    "",
    "## 5. 실전 시나리오별 정리",
    "",
    "### 작업 중 급하게 다른 브랜치로 가야 할 때",
    "```bash",
    "# 현재 작업 임시 저장",
    "git stash",
    "",
    "# 다른 브랜치로 이동",
    "git switch main",
    "",
    "# 급한 작업 후 돌아오기",
    "git switch feature/login",
    "",
    "# 임시 저장한 작업 복원",
    "git stash pop",
    "```",
    "",
    "### 실수로 수정한 파일 되돌리기",
    "```bash",
    "# 특정 파일만 되돌리기",
    "git restore src/App.jsx",
    "",
    "# 모든 수정 파일 되돌리기 (주의: 복구 불가!)",
    "git restore .",
    "",
    "# add한 파일은 먼저 unstage 후 되돌리기",
    "git restore --staged src/App.jsx",
    "git restore src/App.jsx",
    "```",
    "",
    "### 특정 커밋의 파일 하나만 가져오기",
    "```bash",
    "# 커밋 로그에서 해시 확인",
    "git log --oneline",
    "",
    "# 해당 커밋의 파일을 현재로 가져오기",
    "git restore --source abc1234 src/config.js",
    "```",
    "",
    "### 삭제한 파일 복구하기",
    "```bash",
    "# 아직 커밋 안 한 삭제",
    "git restore deleted-file.js",
    "",
    "# 커밋된 삭제 복구 (이전 커밋에서)",
    "git restore --source HEAD~1 deleted-file.js",
    "```",
    "",
    "---",
    "",
    "## checkout vs switch vs restore 비교",
    "",
    "| 동작 | checkout (기존) | switch / restore (신규) |",
    "|------|-----------------|------------------------|",
    "| 브랜치 이동 | `git checkout 브랜치` | `git switch 브랜치` |",
    "| 새 브랜치 | `git checkout -b 이름` | `git switch -c 이름` |",
    "| 이전 브랜치 | `git checkout -` | `git switch -` |",
    "| 파일 복원 | `git checkout -- 파일` | `git restore 파일` |",
    "| add 취소 | `git reset HEAD 파일` | `git restore --staged 파일` |",
    "| 과거 파일 | `git checkout 커밋 -- 파일` | `git restore --source 커밋 파일` |",
    "| 과거 커밋 이동 | `git checkout 해시` | `git switch --detach 해시` |",
    "",
    "> **권장**: 새 프로젝트에서는 `switch`/`restore`를 사용하세요. 역할이 명확하고 실수를 줄여줍니다.",
    "",
    "---",
    "",
    "## 자주 하는 실수와 해결",
    "",
    "| 실수 | 원인 | 해결 |",
    "|------|------|------|",
    "| `error: Your local changes would be overwritten` | 수정 중인 파일이 있는데 브랜치 전환 시도 | `git stash` 후 전환, 또는 `git commit` |",
    "| `detached HEAD` 경고 | 커밋 해시로 checkout | `git switch main`으로 복귀, 작업했다면 `git switch -c 브랜치` |",
    "| `pathspec '파일' did not match` | 파일명/경로 오타 | `git status`로 정확한 경로 확인 |",
    "| 되돌린 파일 복구 | `git restore`로 수정 취소 후 | `git restore`로 날린 변경은 복구 불가 (커밋 안 했다면) |",
    "",
    "---",
    "",
    "### 요약",
    "`git checkout`은 브랜치 전환과 파일 복원을 모두 담당하던 명령어입니다. Git 2.23부터 **`git switch`**(브랜치 전환)와 **`git restore`**(파일 복원)로 분리되었으며, 역할이 명확한 새 명령어 사용을 권장합니다. Detached HEAD 상태는 과거 커밋을 확인할 때 발생하며, 작업을 유지하려면 새 브랜치를 생성해야 합니다."
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
