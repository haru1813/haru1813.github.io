<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "springboot-configuration",
  title: "Spring Boot 설정(Configuration) 완전 정리",
  date: "2026-02-11",
  summary: "application.yml 기본 구조와 프로퍼티 파일 비교, 프로파일 활성화 방법 등 Spring Boot 설정 파일의 기초를 정리합니다.",
  category: "Technology/SpringBoot",
  tags: ["springboot","configuration","yaml","profile","backend"],
  content: [
    "## Spring Boot 설정 파일",
    "",
    "Spring Boot는 `application.yml` 또는 `application.properties`를 기본 설정 파일로 사용합니다.",
    "",
    "```yaml",
    "# application.yml",
    "server:",
    "  port: 8080",
    "",
    "spring:",
    "  application:",
    "    name: my-app",
    "  profiles:",
    "    active: dev",
    "",
    "app:",
    "  name: My Application",
    "  version: 1.0.0",
    "```",
    "",
    "> `${}` 플레이스홀더로 환경 변수를 참조할 수 있습니다: `${DB_HOST:localhost}`",
    "",
    "---",
    "",
    "## yml vs properties",
    "",
    "| 비교 | `application.yml` | `application.properties` |",
    "|------|-------------------|--------------------------|",
    "| 형식 | 계층 구조 (들여쓰기) | key=value (플랫) |",
    "| 가독성 | 중첩 설정에 유리 | 단순 설정에 유리 |",
    "| 다중 프로파일 | `---`로 분리 가능 | 파일 분리 필요 |",
    "",
    "```yaml",
    "# yml (계층 구조)",
    "spring:",
    "  datasource:",
    "    url: jdbc:mysql://localhost:3306/db",
    "    username: admin",
    "```",
    "",
    "```properties",
    "# properties (플랫)",
    "spring.datasource.url=jdbc:mysql://localhost:3306/db",
    "spring.datasource.username=admin",
    "```",
    "",
    "---",
    "",
    "## 프로파일 활성화 방법",
    "",
    "```bash",
    "# 1. application.yml에서 설정",
    "spring.profiles.active: dev",
    "",
    "# 2. JVM 옵션",
    "java -jar app.jar -Dspring.profiles.active=prod",
    "",
    "# 3. 환경 변수",
    "export SPRING_PROFILES_ACTIVE=prod",
    "",
    "# 4. 커맨드라인 인자",
    "java -jar app.jar --spring.profiles.active=prod",
    "```",
    "",
    "프로파일별 설정 파일 (`application-dev.yml`, `application-prod.yml`)이 공통 `application.yml` 위에 오버라이드됩니다."
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
