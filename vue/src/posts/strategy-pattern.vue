<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "strategy-pattern",
  title: "Strategy(전략) 패턴 [행위]",
  date: "2026-02-11",
  summary: "알고리즘을 교체 가능하게 만드는 패턴. 결제 방식, 정렬 방식 등에 사용됩니다.",
  category: "Technology/Design-Pattern",
  tags: ["theory","design-pattern","oop"],
  content: ["## Strategy(전략) 패턴이란?", "Strategy 패턴은 알고리즘(동작)을 교체 가능하게 만드는 패턴입니다. 결제 방식, 정렬 방식, 할인 정책처럼 '어떻게 할지'를 런타임에 바꿔 끼우고 싶을 때 사용합니다.", "", "핵심은 이거예요:", "- Strategy(인터페이스)를 여러 구현체가 구현하고, Context가 그 중 하나를 가지고 있음", "- `if/else`나 `switch`로 분기하는 대신 전략 객체를 주입해서 교체", "", "## 언제 필요할까", "- 같은 목적(결제, 정렬, 할인)에 대해 여러 방식이 있고, 상황에 따라 바꿔 쓰고 싶을 때", "- `if (type == A) ... else if (type == B) ...` 분기가 계속 늘어날 때", "- 알고리즘을 독립적으로 테스트하고 싶을 때", "", "## 장점/단점(현실적으로)", "### 장점", "- 분기문 감소, 가독성 향상", "- 새 전략 추가 시 기존 코드 수정 없이 구현체만 추가", "- 각 전략을 단위 테스트하기 쉬움", "", "### 단점", "- 전략 클래스가 늘어날 수 있음", "- 단순한 경우 람다/함수로 충분할 수 있음", "", "## Java 예제", "결제 방식을 카드/페이팔/암호화폐로 교체하는 예제입니다.", "", "### 1) Strategy(인터페이스)", "```java", "interface PaymentStrategy {", "    void pay(int amount);", "}", "```", "", "### 2) ConcreteStrategy", "```java", "class CardPayment implements PaymentStrategy {", "    public void pay(int amount) {", "        System.out.println(\"카드 결제: \" + amount);", "    }", "}", "class PayPalPayment implements PaymentStrategy {", "    public void pay(int amount) {", "        System.out.println(\"페이팔 결제: \" + amount);", "    }", "}", "```", "", "### 3) Context", "```java", "class Checkout {", "    private PaymentStrategy strategy;", "    public void setStrategy(PaymentStrategy s) { strategy = s; }", "    public void checkout(int amount) {", "        if (strategy == null) throw new IllegalStateException(\"strategy not set\");", "        strategy.pay(amount);", "    }", "}", "```", "", "### 4) Client", "```java", "Checkout c = new Checkout();", "c.setStrategy(new CardPayment());", "c.checkout(10000);", "c.setStrategy(new PayPalPayment());", "c.checkout(5000);", "```", "", "## Unity C# 예제 (게임)", "이동 알고리즘(걷기/달리기/대시)을 런타임에 교체합니다. Shift 키로 달리기 전환이 대표 예시이고, DashStrategy는 필요 시 추가로 사용할 수 있습니다.", "", "### 1) 전략 인터페이스", "```csharp", "using UnityEngine;", "", "public interface IMovementStrategy {", "    void Move(Transform transform, Vector3 direction);", "    float GetCurrentSpeed();", "}", "```", "", "### 2) 구체 전략 - 걷기", "```csharp", "public class WalkStrategy : IMovementStrategy {", "    readonly float _speed = 3f;", "    public void Move(Transform t, Vector3 dir) {", "        if (dir.sqrMagnitude < 0.01f) return;", "        t.position += dir.normalized * _speed * Time.deltaTime;", "    }", "    public float GetCurrentSpeed() => _speed;", "}", "```", "", "### 3) 구체 전략 - 달리기", "```csharp", "public class RunStrategy : IMovementStrategy {", "    readonly float _speed = 7f;", "    public void Move(Transform t, Vector3 dir) {", "        if (dir.sqrMagnitude < 0.01f) return;", "        t.position += dir.normalized * _speed * Time.deltaTime;", "    }", "    public float GetCurrentSpeed() => _speed;", "}", "```", "", "### 4) 구체 전략 - 대시", "```csharp", "public class DashStrategy : IMovementStrategy {", "    readonly float _speed = 15f;", "    public void Move(Transform t, Vector3 dir) {", "        if (dir.sqrMagnitude < 0.01f) return;", "        t.position += dir.normalized * _speed * Time.deltaTime;", "    }", "    public float GetCurrentSpeed() => _speed;", "}", "```", "", "### 5) Context (PlayerController)", "```csharp", "public class PlayerController : MonoBehaviour {", "    IMovementStrategy _strategy = new WalkStrategy();", "", "    public void SetSprint(bool isSprinting) {", "        _strategy = isSprinting ? new RunStrategy() : new WalkStrategy();", "    }", "", "    void Update() {", "        var input = new Vector3(Input.GetAxis(\"Horizontal\"), 0, Input.GetAxis(\"Vertical\"));", "        SetSprint(Input.GetKey(KeyCode.LeftShift));", "        _strategy.Move(transform, input);", "    }", "}", "```", "", "## 정리", "- Strategy는 알고리즘을 객체로 분리해 교체 가능하게 만드는 패턴입니다. Java의 `Comparator`, `ThreadPoolExecutor`의 `RejectedExecutionHandler` 등이 이 패턴을 사용합니다."]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
