<script setup>
import ArticleHeader from '@/components/ArticleHeader.vue'
import ArticleBody from '@/components/ArticleBody.vue'

const post = {
  id: "java-generic",
  title: "Java 제네릭 (Generic)",
  date: "2026-02-17",
  summary: "Java 제네릭의 개념, 제네릭 클래스/메서드, 와일드카드, PECS 원칙, 타입 소거까지 상세히 정리합니다.",
  category: "Coding/Java",
  tags: ["java", "제네릭", "generic", "basics"],
  content: [
    "## 제네릭(Generic)이란?",
    "",
    "제네릭은 **타입을 파라미터화(parameterize)**하는 기능입니다. 클래스나 메서드를 정의할 때 타입을 확정짓지 않고, 사용할 때 구체적인 타입을 지정합니다.",
    "",
    "제네릭을 사용하면 **컴파일 시점에 타입을 체크**할 수 있어 타입 안전성이 보장되고, 불필요한 타입 캐스팅을 제거할 수 있습니다.",
    "",
    "```java",
    "// 제네릭 사용 전: Object 타입으로 처리",
    "List list = new ArrayList();",
    "list.add(\"hello\");",
    "String str = (String) list.get(0); // 캐스팅 필요 + ClassCastException 위험",
    "",
    "// 제네릭 사용 후: 타입을 명시",
    "List<String> list = new ArrayList<>();",
    "list.add(\"hello\");",
    "String str = list.get(0); // 캐스팅 불필요 + 타입 안전",
    "```",
    "",
    "---",
    "",
    "## 제네릭을 사용하는 이유",
    "",
    "| 장점 | 설명 |",
    "|------|------|",
    "| **타입 안전성** | 컴파일 시점에 타입 오류 감지 |",
    "| **캐스팅 제거** | 불필요한 타입 변환 코드 제거 |",
    "| **코드 재사용** | 하나의 코드로 여러 타입에 대응 |",
    "",
    "```java",
    "// 제네릭 없이: 타입 안전성 없음",
    "List list = new ArrayList();",
    "list.add(\"문자열\");",
    "list.add(123);  // 실수로 Integer 추가 → 컴파일 에러 없음",
    "String s = (String) list.get(1); // 런타임에 ClassCastException!",
    "",
    "// 제네릭 사용: 타입 안전성 보장",
    "List<String> list = new ArrayList<>();",
    "list.add(\"문자열\");",
    "// list.add(123);  // 컴파일 에러! Integer를 넣을 수 없음",
    "```",
    "",
    "---",
    "",
    "## 제네릭 클래스 작성",
    "",
    "클래스 이름 뒤에 `<T>`를 붙여 타입 파라미터를 선언합니다.",
    "",
    "```java",
    "public class Box<T> {",
    "    private T content;",
    "",
    "    public void put(T content) {",
    "        this.content = content;",
    "    }",
    "",
    "    public T get() {",
    "        return content;",
    "    }",
    "",
    "    public boolean isEmpty() {",
    "        return content == null;",
    "    }",
    "}",
    "```",
    "",
    "```java",
    "// 사용할 때 구체적인 타입 지정",
    "Box<String> stringBox = new Box<>();",
    "stringBox.put(\"Hello\");",
    "String value = stringBox.get(); // 캐스팅 불필요",
    "",
    "Box<Integer> intBox = new Box<>();",
    "intBox.put(42);",
    "int num = intBox.get();",
    "```",
    "",
    "### 여러 타입 파라미터를 가진 클래스",
    "",
    "```java",
    "public class Pair<K, V> {",
    "    private K key;",
    "    private V value;",
    "",
    "    public Pair(K key, V value) {",
    "        this.key = key;",
    "        this.value = value;",
    "    }",
    "",
    "    public K getKey() { return key; }",
    "    public V getValue() { return value; }",
    "",
    "    @Override",
    "    public String toString() {",
    "        return key + \"=\" + value;",
    "    }",
    "}",
    "",
    "// 사용 예시",
    "Pair<String, Integer> score = new Pair<>(\"홍길동\", 95);",
    "System.out.println(score.getKey());   // 홍길동",
    "System.out.println(score.getValue()); // 95",
    "```",
    "",
    "---",
    "",
    "## 제네릭 메서드 작성",
    "",
    "메서드에도 독립적인 타입 파라미터를 선언할 수 있습니다. 반환 타입 앞에 `<T>`를 작성합니다.",
    "",
    "```java",
    "public class Util {",
    "",
    "    // 제네릭 메서드: <T>를 반환타입 앞에 선언",
    "    public static <T> Box<T> boxing(T content) {",
    "        Box<T> box = new Box<>();",
    "        box.put(content);",
    "        return box;",
    "    }",
    "",
    "    // 두 객체 비교",
    "    public static <K, V> boolean compare(Pair<K, V> p1, Pair<K, V> p2) {",
    "        return p1.getKey().equals(p2.getKey()) &&",
    "               p1.getValue().equals(p2.getValue());",
    "    }",
    "",
    "    // 배열에서 특정 값 찾기",
    "    public static <T> int indexOf(T[] array, T target) {",
    "        for (int i = 0; i < array.length; i++) {",
    "            if (array[i].equals(target)) return i;",
    "        }",
    "        return -1;",
    "    }",
    "}",
    "```",
    "",
    "```java",
    "// 호출 시 타입 추론",
    "Box<String> box = Util.boxing(\"Hello\");       // T → String 추론",
    "Box<Integer> box2 = Util.<Integer>boxing(42);  // 명시적 타입 지정도 가능",
    "",
    "String[] names = {\"Alice\", \"Bob\", \"Charlie\"};",
    "int idx = Util.indexOf(names, \"Bob\"); // 1",
    "```",
    "",
    "---",
    "",
    "## 타입 파라미터 명명 관례",
    "",
    "Java에서는 타입 파라미터에 단일 대문자를 사용하는 관례가 있습니다.",
    "",
    "| 문자 | 의미 | 사용 예시 |",
    "|------|------|-----------|",
    "| `T` | Type (일반 타입) | `Box<T>`, `List<T>` |",
    "| `E` | Element (요소) | `List<E>`, `Set<E>` |",
    "| `K` | Key (키) | `Map<K, V>` |",
    "| `V` | Value (값) | `Map<K, V>` |",
    "| `N` | Number (숫자) | `Calculator<N extends Number>` |",
    "| `S`, `U` | 2번째, 3번째 타입 | `Pair<S, U>` |",
    "| `R` | Return (반환) | `Function<T, R>` |",
    "",
    "---",
    "",
    "## 제한된 타입 파라미터 (Bounded Type Parameter)",
    "",
    "`extends` 키워드로 타입 파라미터의 **상한(upper bound)**을 제한할 수 있습니다.",
    "",
    "```java",
    "// T는 Number 또는 그 하위 클래스만 허용",
    "public class NumberBox<T extends Number> {",
    "    private T value;",
    "",
    "    public NumberBox(T value) {",
    "        this.value = value;",
    "    }",
    "",
    "    public double doubleValue() {",
    "        return value.doubleValue(); // Number의 메서드 사용 가능",
    "    }",
    "",
    "    public int intValue() {",
    "        return value.intValue();",
    "    }",
    "}",
    "```",
    "",
    "```java",
    "NumberBox<Integer> intBox = new NumberBox<>(42);      // ✅",
    "NumberBox<Double> dblBox = new NumberBox<>(3.14);     // ✅",
    "// NumberBox<String> strBox = new NumberBox<>(\"abc\"); // ❌ 컴파일 에러",
    "```",
    "",
    "### 다중 제한 (여러 조건)",
    "",
    "```java",
    "// T는 Number를 상속하면서 Comparable도 구현해야 함",
    "public <T extends Number & Comparable<T>> T max(T a, T b) {",
    "    return a.compareTo(b) >= 0 ? a : b;",
    "}",
    "```",
    "",
    "> **주의**: 클래스는 하나만, 인터페이스는 여러 개 지정 가능합니다. 클래스를 먼저 작성해야 합니다.",
    "",
    "---",
    "",
    "## 제네릭과 상속 (공변성 문제)",
    "",
    "`Integer`는 `Number`의 하위 타입이지만, `List<Integer>`는 `List<Number>`의 하위 타입이 **아닙니다**. 이를 **불공변(invariant)**이라 합니다.",
    "",
    "```java",
    "// Integer는 Number의 하위 타입",
    "Number num = new Integer(10); // ✅",
    "",
    "// 하지만 List<Integer>는 List<Number>의 하위 타입이 아님!",
    "List<Integer> intList = new ArrayList<>();",
    "// List<Number> numList = intList; // ❌ 컴파일 에러",
    "```",
    "",
    "만약 허용된다면 다음과 같은 문제가 생기기 때문입니다:",
    "",
    "```java",
    "// 만약 허용된다고 가정하면...",
    "List<Number> numList = intList;",
    "numList.add(3.14); // Double을 추가 → Integer 리스트에 Double이 들어감!",
    "Integer i = intList.get(0); // ClassCastException 발생!",
    "```",
    "",
    "이 문제를 해결하기 위해 **와일드카드**를 사용합니다.",
    "",
    "---",
    "",
    "## 와일드카드 (?)",
    "",
    "### 1. 비한정 와일드카드: `<?>`",
    "",
    "모든 타입을 허용합니다. 타입에 관계없이 동작하는 메서드에 사용합니다.",
    "",
    "```java",
    "// 모든 타입의 리스트를 출력",
    "public static void printList(List<?> list) {",
    "    for (Object item : list) {",
    "        System.out.println(item);",
    "    }",
    "}",
    "",
    "printList(List.of(1, 2, 3));       // List<Integer>",
    "printList(List.of(\"a\", \"b\"));      // List<String>",
    "```",
    "",
    "### 2. 상한 와일드카드: `<? extends T>`",
    "",
    "T와 T의 **하위 타입**만 허용합니다. 주로 **데이터를 읽을 때** 사용합니다.",
    "",
    "```java",
    "// Number 또는 하위 타입(Integer, Double 등)의 리스트",
    "public static double sum(List<? extends Number> list) {",
    "    double total = 0;",
    "    for (Number n : list) {",
    "        total += n.doubleValue(); // 읽기는 가능",
    "    }",
    "    // list.add(1); // ❌ 쓰기 불가! 정확한 타입을 모르므로",
    "    return total;",
    "}",
    "",
    "sum(List.of(1, 2, 3));       // List<Integer> ✅",
    "sum(List.of(1.1, 2.2));      // List<Double> ✅",
    "```",
    "",
    "### 3. 하한 와일드카드: `<? super T>`",
    "",
    "T와 T의 **상위 타입**만 허용합니다. 주로 **데이터를 쓸 때** 사용합니다.",
    "",
    "```java",
    "// Integer 또는 상위 타입(Number, Object)의 리스트",
    "public static void addNumbers(List<? super Integer> list) {",
    "    list.add(1);   // ✅ 쓰기 가능",
    "    list.add(2);",
    "    list.add(3);",
    "    // Integer n = list.get(0); // ❌ 읽기 시 타입 보장 안 됨",
    "}",
    "",
    "List<Number> numList = new ArrayList<>();",
    "addNumbers(numList); // ✅",
    "",
    "List<Object> objList = new ArrayList<>();",
    "addNumbers(objList); // ✅",
    "```",
    "",
    "---",
    "",
    "## PECS 원칙 (Producer Extends, Consumer Super)",
    "",
    "**PECS**는 와일드카드 사용의 핵심 원칙입니다.",
    "",
    "| 역할 | 와일드카드 | 설명 |",
    "|------|-----------|------|",
    "| **Producer** (데이터 제공) | `? extends T` | 컬렉션에서 데이터를 **꺼낼 때** |",
    "| **Consumer** (데이터 소비) | `? super T` | 컬렉션에 데이터를 **넣을 때** |",
    "",
    "```java",
    "// PECS 예시: src에서 읽어서(Producer) dest에 넣기(Consumer)",
    "public static <T> void copy(List<? super T> dest, List<? extends T> src) {",
    "    for (T item : src) {",
    "        dest.add(item);",
    "    }",
    "}",
    "",
    "List<Integer> integers = List.of(1, 2, 3);",
    "List<Number> numbers = new ArrayList<>();",
    "copy(numbers, integers); // Number super Integer ✅, Integer extends Number ✅",
    "```",
    "",
    "기억법: **\"넣을 땐 super, 꺼낼 땐 extends\"**",
    "",
    "---",
    "",
    "## 제네릭과 컬렉션",
    "",
    "Java 컬렉션 프레임워크는 제네릭을 핵심적으로 사용합니다.",
    "",
    "```java",
    "// List<E>",
    "List<String> names = new ArrayList<>();",
    "names.add(\"홍길동\");",
    "names.add(\"이몽룡\");",
    "",
    "// Set<E>",
    "Set<Integer> uniqueNumbers = new HashSet<>();",
    "uniqueNumbers.add(1);",
    "uniqueNumbers.add(2);",
    "",
    "// Map<K, V>",
    "Map<String, Integer> scores = new HashMap<>();",
    "scores.put(\"수학\", 95);",
    "scores.put(\"영어\", 88);",
    "",
    "// 순회",
    "for (Map.Entry<String, Integer> entry : scores.entrySet()) {",
    "    System.out.printf(\"%s: %d점%n\", entry.getKey(), entry.getValue());",
    "}",
    "```",
    "",
    "---",
    "",
    "## 타입 소거 (Type Erasure)",
    "",
    "Java의 제네릭은 **컴파일 시점에만 존재**하고, 런타임에는 타입 정보가 지워집니다. 이를 **타입 소거(Type Erasure)**라 합니다.",
    "",
    "```java",
    "// 컴파일 전",
    "List<String> list = new ArrayList<>();",
    "list.add(\"hello\");",
    "String s = list.get(0);",
    "",
    "// 컴파일 후 (타입 소거)",
    "List list = new ArrayList();",
    "list.add(\"hello\");",
    "String s = (String) list.get(0); // 컴파일러가 캐스팅 코드 삽입",
    "```",
    "",
    "타입 소거는 Java 5 이전 코드와의 **하위 호환성(backward compatibility)**을 위한 설계입니다.",
    "",
    "---",
    "",
    "## 제네릭의 제한사항",
    "",
    "타입 소거로 인해 발생하는 제한사항입니다.",
    "",
    "| 제한사항 | 예시 | 이유 |",
    "|----------|------|------|",
    "| 기본 타입 사용 불가 | `List<int>` ❌ | 타입 소거 후 Object로 변환 불가 |",
    "| `instanceof` 사용 불가 | `obj instanceof List<String>` ❌ | 런타임에 타입 정보 없음 |",
    "| `new T()` 불가 | `T obj = new T()` ❌ | 런타임에 T의 실제 타입 모름 |",
    "| `new T[]` 불가 | `T[] arr = new T[10]` ❌ | 제네릭 배열 생성 불가 |",
    "| `static` 필드에 타입 파라미터 불가 | `static T field` ❌ | 인스턴스마다 T가 다름 |",
    "",
    "```java",
    "// 기본 타입 → 래퍼 클래스 사용",
    "// List<int> list = new ArrayList<>();    // ❌",
    "List<Integer> list = new ArrayList<>();   // ✅",
    "",
    "// instanceof → 와일드카드 사용",
    "// if (obj instanceof List<String>) {}    // ❌",
    "if (obj instanceof List<?>) {}            // ✅",
    "```",
    "",
    "---",
    "",
    "## 제네릭 인터페이스",
    "",
    "인터페이스에도 제네릭을 적용할 수 있습니다.",
    "",
    "```java",
    "public interface Repository<T> {",
    "    void save(T entity);",
    "    T findById(int id);",
    "    List<T> findAll();",
    "    void delete(T entity);",
    "}",
    "",
    "public class UserRepository implements Repository<User> {",
    "    private List<User> users = new ArrayList<>();",
    "",
    "    @Override",
    "    public void save(User user) {",
    "        users.add(user);",
    "    }",
    "",
    "    @Override",
    "    public User findById(int id) {",
    "        return users.stream()",
    "            .filter(u -> u.getId() == id)",
    "            .findFirst()",
    "            .orElse(null);",
    "    }",
    "",
    "    @Override",
    "    public List<User> findAll() {",
    "        return Collections.unmodifiableList(users);",
    "    }",
    "",
    "    @Override",
    "    public void delete(User user) {",
    "        users.remove(user);",
    "    }",
    "}",
    "```",
    "",
    "---",
    "",
    "## Comparable<T> 구현 예제",
    "",
    "제네릭 인터페이스의 대표적 예시인 `Comparable<T>`를 구현하여 객체 정렬을 할 수 있습니다.",
    "",
    "```java",
    "public class Student implements Comparable<Student> {",
    "    private String name;",
    "    private int score;",
    "",
    "    public Student(String name, int score) {",
    "        this.name = name;",
    "        this.score = score;",
    "    }",
    "",
    "    @Override",
    "    public int compareTo(Student other) {",
    "        return Integer.compare(this.score, other.score);",
    "    }",
    "",
    "    @Override",
    "    public String toString() {",
    "        return name + \"(\" + score + \"점)\";",
    "    }",
    "}",
    "```",
    "",
    "```java",
    "List<Student> students = new ArrayList<>();",
    "students.add(new Student(\"홍길동\", 85));",
    "students.add(new Student(\"이몽룡\", 92));",
    "students.add(new Student(\"성춘향\", 78));",
    "",
    "Collections.sort(students);",
    "System.out.println(students);",
    "// [성춘향(78점), 홍길동(85점), 이몽룡(92점)]",
    "```",
    "",
    "---",
    "",
    "## 실전 예제: 제네릭 스택",
    "",
    "```java",
    "public class GenericStack<T> {",
    "    private Object[] elements;",
    "    private int size = 0;",
    "    private static final int DEFAULT_CAPACITY = 10;",
    "",
    "    @SuppressWarnings(\"unchecked\")",
    "    public GenericStack() {",
    "        elements = new Object[DEFAULT_CAPACITY];",
    "    }",
    "",
    "    public void push(T item) {",
    "        ensureCapacity();",
    "        elements[size++] = item;",
    "    }",
    "",
    "    @SuppressWarnings(\"unchecked\")",
    "    public T pop() {",
    "        if (isEmpty()) {",
    "            throw new IllegalStateException(\"스택이 비어 있습니다.\");",
    "        }",
    "        T item = (T) elements[--size];",
    "        elements[size] = null;",
    "        return item;",
    "    }",
    "",
    "    @SuppressWarnings(\"unchecked\")",
    "    public T peek() {",
    "        if (isEmpty()) {",
    "            throw new IllegalStateException(\"스택이 비어 있습니다.\");",
    "        }",
    "        return (T) elements[size - 1];",
    "    }",
    "",
    "    public boolean isEmpty() {",
    "        return size == 0;",
    "    }",
    "",
    "    public int size() {",
    "        return size;",
    "    }",
    "",
    "    private void ensureCapacity() {",
    "        if (size == elements.length) {",
    "            elements = java.util.Arrays.copyOf(elements, size * 2);",
    "        }",
    "    }",
    "}",
    "```",
    "",
    "```java",
    "// 사용 예시",
    "GenericStack<String> stack = new GenericStack<>();",
    "stack.push(\"첫 번째\");",
    "stack.push(\"두 번째\");",
    "stack.push(\"세 번째\");",
    "",
    "System.out.println(stack.peek()); // 세 번째",
    "System.out.println(stack.pop());  // 세 번째",
    "System.out.println(stack.pop());  // 두 번째",
    "System.out.println(stack.size()); // 1",
    "```",
    "",
    "---",
    "",
    "## 💡 팁과 주의사항",
    "",
    "- **다이아몬드 연산자(`<>`)를 활용하세요.** Java 7부터 오른쪽의 타입 인자를 생략할 수 있습니다: `new ArrayList<>()`",
    "- **raw type 사용을 피하세요.** `List list` 대신 `List<?>`나 `List<Object>`를 사용하세요.",
    "- **PECS 원칙을 기억하세요.** 데이터를 꺼내면 `extends`, 넣으면 `super`.",
    "- **기본 타입은 래퍼 클래스를 사용하세요.** `List<int>` 대신 `List<Integer>`.",
    "- **`@SuppressWarnings(\"unchecked\")`는 타입 안전성이 확인된 경우에만 사용하세요.** 무분별한 사용은 런타임 오류의 원인이 됩니다.",
    "- **제네릭 배열 대신 `List<T>`를 사용하세요.** 배열은 공변(covariant)이지만 제네릭은 불공변(invariant)이라 함께 쓰면 타입 안전성이 깨집니다.",
    "",
    "---",
    "",
    "## 요약",
    "",
    "| 핵심 포인트 | 설명 |",
    "|-------------|------|",
    "| 제네릭 클래스 | `class Box<T> { }` |",
    "| 제네릭 메서드 | `<T> T method(T param)` |",
    "| 제한된 타입 | `<T extends Number>` |",
    "| 와일드카드 `?` | 비한정 / 상한(`extends`) / 하한(`super`) |",
    "| PECS | Producer → `extends`, Consumer → `super` |",
    "| 타입 소거 | 런타임에 제네릭 정보 제거 |",
    "| 관례 | T, E, K, V, N, S, R |",
    "| 컬렉션 | `List<T>`, `Map<K,V>`, `Set<E>` |"
  ]
}
</script>

<template>
  <ArticleHeader :post="post" />
  <ArticleBody :content="post.content" />
</template>
